## [BLOCKER] Q1 SQJ431EP pad mapping shorts BAT_F, VIN and GND together (drain tab unconnected, gate floating)
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist mainboard.py line 83-84: Q1 fp Package_SO:PowerPAK_SO-8_Single, pins=[(1,'S','BAT_F'),(2,'D','VIN'),(3,'G','GND')]. gen_kicad.py maps pin numbers 1:1 to footprint pads (lib_symbol is generic, lines 49-69). mainboard.kicad_pcb confirms the KiCad library footprint is used: pad 1 (-2.67,-1.905)=BAT_F, pad 2 (-2.67,-0.635)=VIN, pad 3 (-2.67,0.635)=GND, pad 4 no net, pad 5 (tab 3.81x3.91 + right-side leads) no net. Vishay SQJ431EP PowerPAK SO-8 pinout: pins 1,2,3 = SOURCE, pin 4 = GATE, pins 5-8 = DRAIN (tab). So the three source leads tie BAT_F, VIN and GND to one piece of metal: battery + is a dead short to GND through the fuse F1 the moment the XT60 is plugged in; VIN is also shorted to GND. Even with the pads re-numbered, the intended orientation (S=battery, D=load) is backwards for a P-FET RPP: body diode (anode=D, cathode=S) would conduct a reversed pack straight into VIN. Correct RPP: D=BAT_F, S=VIN, G=GND (body diode conducts battery->load at power-up, VGS=-VIN enhances it; reversed pack: body diode reverse-biased, VGS=0, off).
REC: Replace line 83-84 with: A(dict(ref="Q1", value="SQJ431EP P-FET RPP", kind="ic3", fp="Package_SO:PowerPAK_SO-8_Single", at=(-1,-26), rot=0, side="F", pins=[(1,"S","VIN"),(2,"S","VIN"),(3,"S","VIN"),(4,"G","GND"),(5,"D","BAT_F")])). Regenerate, then confirm in the PCB that pad 5 (tab) = BAT_F and pads 1-3 = VIN.
CORRECTED: In C:\Users\james\BB8\hardware\netlist\mainboard.py replace lines 83-84 with:
A(dict(ref="Q1", value="Si7461DP P-FET RPP -60V 14.5mR", kind="ic3", fp="Package_SO:PowerPAK_SO-8_Single",
       at=(-1, -26), rot=0, side="F", pins=[(1,"S","VIN"),(2,"S","VIN"),(3,"S","VIN"),(4,"G","GND"),(5,"D","BAT_F")]))
Rationale: Vishay Si7461DP-T1-GE3 (-60 V, RDS(on) 14.5 mOhm @ -10 V, ID -14.4 A, VGS +/-20 V, standard PowerPAK SO-8, pins S1-3/G4/D5-8) fits the footprint already placed and routed on the board, so only pad nets change (5 A -> 0.36 W). Do NOT keep SQJ431EP (213 mOhm, 12 A; and it is a PowerPAK SO-8L part needing Package_SO:PowerPAK_SO-8L_Single, not the SO-8 pattern). Update C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv lines 12-13: Q1 = SI7461DP-T1-GE3, PowerPAK SO-8; delete the "30A P-FET (SQJ431EP)" note. Optional robustness (not fab-blocking): 10k gate resistor G->GND and 12 V zener S->G, since SMBJ15A can clamp VIN to ~24 V which exceeds VGS +/-20 V. Apply the netlist change WITHOUT a full regenerate to preserve routing: in KiCad open mainboard.kicad_pcb, edit Q1 (line 225) pad nets directly — pads 1,2,3 -> VIN, pad 4 -> GND, all five pads numbered "5" -> BAT_F — then re-run DRC/connectivity and re-route the short BAT_F-to-tab / VIN-to-pads-1-3 stubs; regenerate only the schematic (or regenerate to a scratch path and diff) so the netlist and PCB agree. Verify in the PCB that pad 5 (tab) = BAT_F, pads 1-3 = VIN, pad 4 = GND before plotting Gerbers.
VERIFIER: CLAIM CONFIRMED from files. (1) C:\Users\james\BB8\hardware\netlist\mainboard.py lines 83-84 are exactly as cited: fp="Package_SO:PowerPAK_SO-8_Single", pins=[(1,"S","BAT_F"),(2,"D","VIN"),(3,"G","GND")]. (2) C:\Users\james\BB8\tools\hw\gen_kicad.py lines 148-154/451-454 embed the installed KiCad library footprint when present (it is: C:\Program Files\KiCad\10.0\share\kicad\footprints\Package_SO.pretty\PowerPAK_SO-8_Single.kicad_mod), and embed_lib_footprint lines 165,176-180 assign nets purely by pad-number string. (3) C:\Users\james\BB8\hardware\kicad\mainboard.kicad_pcb line 225 (Q1 footprint) confirms: pad 1 (-2.67,-1.905)=BAT_F, pad 2 (-2.67,-0.635)=VIN, pad 3 (-2.67,0.635)=GND, pad 4 (-2.67,1.905) no net, and all five pads numbered "5" (3.81x3.91 tab at (0.69,0) plus four leads at x=2.795) have no net. (4) Vishay SQJ431EP datasheet (doc 67033 rev B, p.1 pin diagram) shows S1, S2, S3, G4, D=tab — so pads 1,2,3 are one internal source node: once Q1 is soldered, BAT_F, VIN and GND are shorted together; F1 (10 A blade, netlist line 81-82) sees the 20 A BMS pack directly and blows at first XT60 insertion; the drain tab carries no net and the gate floats. Blocker stands. (5) Orientation analysis is correct: P-FET body diode anode=D cathode=S; correct high-side RPP is D=BAT_F, S=VIN, G=GND (normal: body diode precharges VIN then VGS=-VIN=-9..-12.6 V enhances; reversed: body diode reverse-biased, VGS=0, off). The netlist's S=BAT_F/D=VIN would pass a reversed pack into VIN via the body diode. RECOMMENDATION DEFECTS: (a) Wrong footprint for the named part: datasheet p.1 header and Ordering Information state the package is PowerPAK SO-8L, and KiCad 10 ships a separate Package_SO:PowerPAK_SO-8L_Single (pads 1-4 at x=-2.715 0.72x0.41; "5" tab 3.99x3.96 at x=1.08; only two 0.41x0.47 side stubs at y=+/-2.215, no four right-hand leads). The SO-8 pattern is not the correct land pattern for SQJ431EP. (b) Wrong part: SQJ431EP is a 200 V automotive FET with RDS(on) 0.213 ohm max @ VGS=-10 V (p.2), ID -12 A, RthJA 65 C/W; BOM line 12's note "30A P-FET (SQJ431EP)" is false. Arithmetic: VIN load ~5 A (PS1 5V/5A -> ~2.3 A @12 V, PS2 7.5V/2.5A -> ~1.7 A, amp ~1-2 A) -> P = 0.213*25 = 5.3 W, ~1.0 V drop, >300 C rise; at the 10 A F1 rating 21 W; even 2 A = 0.85 W -> ~55 C rise. A pad-map-only fix yields a board that works at light load then overheats. (c) Process hazard: gen_kicad.py rewrites the .kicad_pcb unrouted (docstring lines 8-11), so "Regenerate" as written discards the autorouted board from commits bf32721/850cb81. Confidence: high on (1)-(5) and (a); high on (b) numbers (datasheet read directly), medium on the ~5 A load estimate.

## [BLOCKER] F2 and F4: BOM part 0451003.MRL (Littelfuse NANO2) does not fit the Fuse_1206_3216Metric footprint
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist line 162 (F2) and line 100 (F4): fp 'Fuse:Fuse_1206_3216Metric'; kicad_pcb pads 1.25 x 1.75 mm at x = +/-1.4 mm (1206 body 3.2 x 1.6 mm). BOM rows F2, F4: 0451003.MRL, package 'Nano2'. Littelfuse 451 series NANO2 body is 6.10 x 2.69 x 2.69 mm with end-cap terminals ~1.3 mm wide; its pads sit ~5-6 mm apart. It cannot be soldered to 1206 pads 2.8 mm apart. Ratings for the record: 0451003.MRL = 3 A, 125 V AC/DC, very fast acting, I2t ~ 1.3 A2s (so it is massively over-rated for 12.6 / 14.4 / 19 / 24 V - voltage is not the problem, the package is). Generic 1206 chip fuses are rated 24-63 V depending on series (e.g. Littelfuse 467 series 3 A = 32 V; Bussmann 3216FF3-R = 63 V); either is fine for the 24.4 V SMBJ15A clamp but you must pick one and update the BOM.
REC: Either (a) keep the BOM and change both footprints: two("F2","3A fuse","fuse","Fuse:Fuse_Littelfuse_NANO2_451_453",(-40,18),"CHG+","CHG_F") and the same fp for F4 (line 100) - re-check courtyards at (-40,18) and (56,24) because the body is ~2x longer; or (b) keep the 1206 footprint and change the BOM MPN to a 1206 chip fuse rated >= 32 V, 3 A (e.g. Littelfuse 0467003.ER or Bussmann 3216FF3-R), value string '3A 1206 32V'.
CORRECTED: Pick ONE of:
(a) Keep BOM 0451003.MRL and fix the footprint name to the real KiCad 10 library file (hyphens, not underscores). mainboard.py line 100: A(two("F4", "3A fuse", "fuse", "Fuse:Fuse_Littelfuse-NANO2-451_453", (56, 24), "VIN", "AMP_12V_F")) and line 162: A(two("F2", "3A fuse", "fuse", "Fuse:Fuse_Littelfuse-NANO2-451_453", (-40, 18), "CHG+", "CHG_F")). Verify after regenerating that gen_kicad.py's summary line reports the library-footprint count went UP by 2 and no "[warn] F2/F4: library footprint failed" appears on stderr (a typo silently falls back to a generic footprint, gen_kicad.py lines 451-457). NANO2 courtyard is +/-3.69 x +/-1.83 mm; at the current spaced placement (F2 120.22,110.28; F4 226.0,126.0) nothing is within 6 mm, so no courtyard conflict. Update the BOM 'package' text from 'Nano2' to 'NANO2 451/453 SMF' for clarity.
(b) Keep Fuse:Fuse_1206_3216Metric and change BOM lines 19 and 21 MPN from 0451003.MRL / Nano2 to a 1206 chip fuse, 3 A, >= 24 VDC (e.g. Littelfuse 0467003.ER or Bussmann 3216FF3-R — confirm the voltage rating on the distributor listing before ordering), package '1206', value string '3A 1206'.
Do NOT use the string "Fuse:Fuse_Littelfuse_NANO2_451_453" — that file does not exist in Fuse.pretty.
VERIFIER: The claim is confirmed from the files; the recommendation contains one fab-critical error (wrong footprint name) that must be corrected.

EVIDENCE VERIFIED:
- C:\Users\james\BB8\hardware\netlist\mainboard.py line 100: A(two("F4", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", (56, 24), "VIN", "AMP_12V_F")); line 162: A(two("F2", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", (-40, 18), "CHG+", "CHG_F")). Exactly as cited.
- C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv line 19: "F2,1,Fuse 3A (charge path),0451003.MRL,Nano2,..." and line 21: "F4,1,Fuse 3A (amp 12V),0451003.MRL,Nano2,...". Exactly as cited. (F3 line 20 MF-MSMF300 / 1812 matches its Fuse_1812 footprint, so F3 is fine.)
- C:\Users\james\BB8\hardware\kicad\mainboard.kicad_pcb: both F2 and F4 are embedded as footprint "Fuse_1206_3216Metric" with pad "1" at (-1.4 0) size 1.25 x 1.75 and pad "2" at (1.4 0) size 1.25 x 1.75 — the auditor's numbers are exact. Same footprint in HEAD (git show HEAD: 2x Fuse_1206_3216Metric).
- KiCad 10 library C:\Program Files\KiCad\10.0\share\kicad\footprints\Fuse.pretty\Fuse_Littelfuse-NANO2-451_453.kicad_mod (the correct land pattern for 0451003.MRL): pads at (-2.455 0) and (2.455 0), size 1.96 x 3.15, courtyard +/-3.69 x +/-1.83. Arithmetic: 1206 pad outer edges span 2*(1.4+0.625)=4.05 mm; NANO2 pad inner edges are at +/-(2.455-0.98)=+/-1.475 mm and the body is ~6.1 mm long, so the NANO2 end caps fall outside the 1206 pads entirely. Not solderable. The auditor's "~5-6 mm apart" is loose (pad centres are 4.91 mm apart) but the conclusion is correct.
- Design doc (PCB_v10_Design.md) never names F2/F4 or a package; lines 371/426/440 just say "3 A fuse". So the doc does not resolve the conflict either way.

RECOMMENDATION CHECK — option (a) is WRONG AS WRITTEN and would introduce a new fab problem: the auditor wrote "Fuse:Fuse_Littelfuse_NANO2_451_453" (underscores). The real library file is "Fuse_Littelfuse-NANO2-451_453" (hyphens). gen_kicad.py lines 148-154 (find_lib_footprint) do an exact os.path.exists(lib.pretty/name.kicad_mod) lookup, and lines 451-457 silently fall back to generic_footprint() on a miss (only the summary count at line 577 changes). So the underscored name would regenerate the board with a guessed generic 2-pad footprint — arguably worse than the current state. Courtyard fit for the corrected name: in the current PCB F2 is at (120.22,110.28) with nearest neighbour R27 at (126.64,107.43) (6.4 mm away) and F4 at (226.0,126.0) with J10 at (227,136) (10 mm) and L1 at (209.25,115.03); the NANO2 courtyard half-width of 3.69 mm clears all of them, so no collision at the spaced-2.5x placement. Note the working-copy kicad_pcb is a fresh regeneration (0 segments vs 887 in HEAD), so the board will be re-routed anyway and the footprint change flows through gen_kicad.py cleanly.

Option (b) is sound: a 1206 3 A chip fuse rated >= 24 V (VIN max 12.6 V; SMBJ15A clamp 24.4 V transient only). I am moderately (not fully) certain of the exact voltage ratings quoted for 0467003.ER (32 V) and 3216FF3-R (63 V) — confirm on the Digi-Key listing before ordering, but any 1206 3 A fuse with >= 24 VDC rating is adequate. Current rating question (MAX9744 2x20 W peaks vs 3 A) is outside this finding and matches the doc's own spec (line 440).

Severity "blocker" is justified: two unpopulatable fuses in series with the charge path and the amp rail means assembly cannot complete as documented.

## [BLOCKER] CHG_SENSE cannot detect a charger at all: CHG+ is hard-wired to the pack, so CHG_SENSE == VBAT_SENSE whenever the pack is plugged in
LENS: ESP32 ADC INPUTS (CHG_SENSE / GPIO39-VN, VBAT_SENSE / GPIO35-D35)
EVIDENCE: mainboard.py L161 J11 pin3 -> net CHG+; L162 F2 CHG+ -> CHG_F; L164 J12 pin1 = CHG_F -> pack charge lead; L165-166 R26/R27 divide CHG+. PCB_v10_Design.md §14.2 L371: 'CHG+ -> 3 A fuse -> directly to pack + (common-port BMS - no series diode)'. With a COMMON-PORT BMS the charge lead IS P+, i.e. the same terminal as J1 BAT+ (L80). So CHG+ = BAT+ (minus F2 drop) = VIN + (F1+Q1 drop ~0.1 V) at all times. Arithmetic: pack 9.0-12.6 V -> CHG_SENSE 1.58-2.21 V with NO charger; with a 12.6 V CC/CV charger attached the pack terminal is still <= 12.6 V -> CHG_SENSE <= 2.21 V. The only charger signature is the IR drop across slip ring + F2 + wire at 1 A (~0.1-0.3 ohm -> 0.1-0.3 V -> 18-53 mV at the node -> 22-65 LSB at 0.8 mV/LSB) against ESP32 ADC noise of ~10-12 LSB raw plus two channels with independent offset - not a usable 'charger present' detector. Confirmed in the routed PCB (HEAD hardware/kicad/mainboard.kicad_pcb): CHG_SENSE net exists and R26 pad1 = CHG+, R27 pad2 = GND, nothing isolates CHG+ from CHG_F/J12. §4.2 L134 and §4.1 L99 state the intent: 'charger present -> drive force-disabled'. Both D3 (SS54) and U8 (LM74610) that would create the isolation are in the BOM (rows 9 and 16) but absent from the netlist.
REC: Pre-fab only fix (needs a footprint). Minimum-risk version, SS54 in series between the fused charger side and the pack, sense kept on the charger side: (1) change L164 to J12 pins=[(1,"+","CHG_PACK"),(2,"-","CHG-")]; (2) add A(two("D3", "SS54", "diode", "Diode_SMD:D_SMC", (-46, 14), "CHG_PACK", "CHG_F"))  # KiCad D_SMC pad1 = CATHODE -> pack side, pad2 = ANODE -> CHG_F (charger side); (3) add a bleed so SS54 reverse leakage (up to ~0.1-0.2 mA hot at 12.6 V) cannot lift the open charger side: A(two("R33", "4.7k", "res", "Resistor_SMD:R_0805_2012Metric", (-33, 15), "CHG+", "GND"))  # 34 mW while charging; leakage 0.2 mA x (4.7k||57k = 4.34k) = 0.87 V on CHG+ -> 0.15 V at CHG_SENSE, so firmware threshold 'charger present' = node > 1.3 V (CHG+ > 7.4 V) is safe. Cost: pack charges to 12.6 - Vf(SS54 at the charger's ~100 mA termination current ~0.25 V) = ~12.35 V = 4.12 V/cell (~93-95 % SoC), which §14.2 rejected; if that loss is unacceptable use U8 LM74610 + N-FET ideal diode (~20-30 mV drop) in the same position with CHG+ sensed on its anode side - verify the LM74610 VSSOP-8 pin map against the TI datasheet before adding it (not verified here). If neither is added, the charge-lock feature must be dropped and GPIO39 repurposed; do not ship firmware that trusts CHG_SENSE as netlisted. Add CHG_PACK to the Power netclass (it already matches pattern 'CHG*').
CORRECTED: Keep the auditor's structure (series SS54 between charger side and pack, sense kept on the charger side) with these corrections: (1) mainboard.py L164: J12 pins=[(1,"+","CHG_PACK"),(2,"-","CHG-")]. (2) add A(two("D3","SS54","diode","Diode_SMD:D_SMC",(-46,14),"CHG_PACK","CHG_F"))  # KiCad D_SMC pad1=K->pack, pad2=A->CHG_F (verified in KiCad 10.0 lib). (3) bleed: use 2.2k, not 4.7k, and 1206: A(two("R33","2.2k","res","Resistor_SMD:R_1206_3216Metric",(-33,15),"CHG+","GND"))  # 72 mW while charging; SS54 datasheet leakage is 0.5 mA@25C/20 mA@100C (40 V) - at 2 mA hot: 2 mA x (2.2k||57k) = 4.2 V on CHG+ -> 0.74 V at CHG_SENSE, safe below a 1.3 V (CHG+ > 7.4 V) firmware threshold; with 4.7k the same 2 mA gives 1.52 V = false lock. (4) After regenerating, confirm gen_kicad.py reports the "library footprints embedded" count up by 2 (D_SMC has no generic fallback at L277 - a lookup failure silently yields a 2-pad through-hole stub at L308-311). (5) Give D3 pads a small copper pour / wide 1.2 mm Power-class tracks (Vf x 3 A = ~1.5 W worst case with a 3 A charger; 0.35 W at the 1 A nominal). (6) Firmware: charger present = CHG_SENSE > 1.3 V with hysteresis (release < 0.9 V); accept pack termination at ~12.35 V (4.12 V/cell, ~93-95 % SoC); if that is unacceptable, replace D3 with LM74610 + N-FET in the same position with CHG+ sensed on the anode side - pinout not verified here. (7) Separately fit the BOM's D4 BAT54S clamp on CHG_SENSE/VBAT_SENSE (a reversed charger at the shell port now drives CHG+ negative through R26 into GPIO39). (8) Independently verify D2 SS14 (L94) orientation - by the same pad-1=cathode convention it is wired RP_5V -> +5V_LOGIC, opposite to its documented purpose.
VERIFIER: EVIDENCE VERIFIED against C:\Users\james\BB8\hardware\netlist\mainboard.py: L161 J11 pin3 "CHG+"->net CHG+; L162 F2 "3A fuse" CHG+ -> CHG_F; L164 J12 "Charger XT30" pin1 "+" = CHG_F, pin2 "-" = CHG-; L165/166 R26 47k CHG+->CHG_SENSE, R27 10k CHG_SENSE->GND; L13 DEVKIT_NETS "VN":"CHG_SENSE", "D35":"VBAT_SENSE"; L87/88 R24/R25 VIN->VBAT_SENSE. No D3/D4/U8 anywhere in the netlist (grep). PCB_v10_Design.md §14.2 L371 literally says "CHG+ -> 3 A fuse -> directly to pack + (common-port BMS - no series diode...)"; §4.2 L134 "CHG_SENSE (charger present) ... charging -> drive force-disabled"; §4.1 L99 "(motors locked while charging)"; §10 L269 makes the charge-lock a spec item. BOM rows 16 (D3 SS54 "Charge path reverse block (if no U8)"), 17 (D4 BAT54S), 9 (U8 LM74610) exist but are not in the netlist. Grep of firmware/ finds no CHG_SENSE/GPIO39 code yet, so no shipped firmware trusts it today, but the hardware cannot deliver the documented feature.

LOGIC: with a common-port BMS the pack "charge lead" IS P+/P- (same terminals as J1), so once the pack is plugged into J12, CHG+ = BAT+ (minus zero-current F2) permanently. CHG_SENSE therefore tracks pack voltage whether or not a charger is attached. Arithmetic checks: 9.0 V x 10/57 = 1.579 V, 12.6 x 10/57 = 2.211 V; a 12.6 V CC/CV charger cannot push the node above ~2.21 V + IR. The auditor's "signature" estimate is actually generous: the slip ring sits UPSTREAM of the CHG+ node (shell port -> slip ring -> J11 -> CHG+), so it contributes nothing; the only IR between CHG+ and pack+ is F2 (1206 3 A fuse, tens of mOhm) + XT30 + pack wire, i.e. ~50-100 mV at 1 A -> 9-18 mV at the node -> ~11-22 LSB at 0.8 mV/LSB. Against that, 1 % divider mismatch between R24/R25 and R26/R27 alone is up to ~2 % of 2.2 V = ~44 mV = ~55 LSB static offset, plus ESP32 per-channel ADC error. Not a usable detector; the claim stands. (Severity is arguably "major" rather than "blocker" - the failure mode is a missing safety interlock, not damage - but it is pre-fab-only, so treating it as a blocker is defensible.)

RECOMMENDATION CHECK: (a) KiCad 10.0 library Diode_SMD:D_SMC is installed (C:\Program Files\KiCad\10.0\share\kicad\footprints\Diode_SMD.pretty\D_SMC.kicad_mod); its pad 1 is at x=-3.4 next to the silk polarity bar at x=-4.91 -> pad 1 = cathode, confirming the auditor's pin assumption; two("D3",...,"CHG_PACK","CHG_F") gives K=CHG_PACK, A=CHG_F, charger current A->K = correct direction. Note gen_kicad.py L277 has a generic fallback only for D_SMA/D_SMB - D_SMC has NONE, so if the library lookup ever fails it silently falls to the "unknown" 2-pad 2.54 mm through-hole stub (L308-311); check the generator's "library footprints embedded" count (L579) rises by 2 after adding D3 + R33. R_0805_2012Metric also exists in the library. (b) Netclass patterns are passed to KiCad as netclass_patterns (gen_kicad.py L561); "CHG*" matches CHG_PACK. (c) The bleed value is the weak spot: the auditor assumes SS54 hot leakage 0.1-0.2 mA, but the Vishay SS54 datasheet max is 0.5 mA at 25 C / 20 mA at 100 C (at 40 V); at 12.6 V and a 60-70 C enclosure 1-2 mA is plausible. 2 mA x (4.7k||57k = 4.34k) = 8.7 V on CHG+ -> 1.52 V at the node -> false "charger present" at their 1.3 V threshold -> drive locked on a hot day. 2.2k fixes this (2 mA x 2.12k = 4.2 V -> 0.74 V node; 12.6^2/2.2k = 72 mW while charging, fine on 1206, marginal on 0805). (d) SS54 thermal: Vf ~0.45-0.5 V at 3 A -> ~1.5 W on an SMC pad; fine at the 1 A nominal charger (0.35 W) but give D3 a copper pour if a 3 A charger is used. (e) Reversed charger polarity now cannot reach the pack (bonus), but CHG+ goes negative and pulls GPIO39 below GND through 47k (~0.27 mA) - the BOM's D4 BAT54S clamp is still missing; separate finding.

ASIDE (out of this lens, found while verifying the pad convention, flag to the diode/power lens): by the same KiCad pad-1=cathode convention, D2 SS14 at L94 two("D2","SS14",...,"+5V_LOGIC","RP_5V") has K=+5V_LOGIC, A=RP_5V, i.e. it conducts RP_5V -> +5V_LOGIC - the opposite of the BOM/doc intent "+5V_LOGIC -> RP2350-Zero 5V (USB back-feed block)". The routed PCB embeds library D_SMA with that net assignment. The RP2350-Zero would get no 5 V from the buck and a laptop on its USB-C would back-feed +5V_LOGIC. Should be verified independently before fab.

## [BLOCKER] D2 SS14 is installed backwards: buck cannot feed the RP2350-Zero and the Zero's USB DOES back-feed +5V_LOGIC (out of lens, found while checking diode pin conventions)
LENS: ESP32 ADC INPUTS (CHG_SENSE / GPIO39-VN, VBAT_SENSE / GPIO35-D35)
EVIDENCE: mainboard.py L94: two("D2", "SS14", "diode", "Diode_SMD:D_SMA", (0, 48), "+5V_LOGIC", "RP_5V") -> pin1 = +5V_LOGIC, pin2 = RP_5V. KiCad Diode_SMD:D_SMA pad1 is the CATHODE (cathode bar on F.SilkS at x = -3.51, pad1 at x = -2; KiCad library convention pin1 = K). So K = +5V_LOGIC, A = RP_5V: conducts from RP_5V into +5V_LOGIC, blocks +5V_LOGIC -> RP_5V. Generated PCB confirms: D2 pad1 net +5V_LOGIC, pad2 net RP_5V. Intent per BOM row 15 and §4.1 L96/L103 is the opposite ('+5V_LOGIC -> RP2350-Zero 5V via SS14', 'SS14 blocks back-feed'). Cross-check: D1 SMBJ15A on L85 uses pin1 = VIN (K) / pin2 = GND (A), which is the correct TVS orientation under the same convention, so the convention is pin1 = K and D2 alone is reversed. Result: RP2350-Zero unpowered from the pack; a laptop on the Zero's USB-C pushes ~5 V into the whole +5V_LOGIC rail through a 1 A diode.
REC: L94 -> A(two("D2", "SS14", "diode", "Diode_SMD:D_SMA", (0, 48), "RP_5V", "+5V_LOGIC"))  # pin1 K = RP_5V, pin2 A = +5V_LOGIC. Regenerate and re-check D2's silkscreen bar points at the RP_5V side.
CORRECTED: mainboard.py L94 -> A(two("D2", "SS14", "diode", "Diode_SMD:D_SMA", (0, 48), "RP_5V", "+5V_LOGIC"))  # pin1 = K = RP_5V, pin2 = A = +5V_LOGIC. Then EITHER regenerate (accepting loss of the ~95% manual routing) OR, on the existing routed mainboard.kicad_pcb: regenerate the schematic, "Update PCB from Schematic" (pad nets swap), and rotate footprint D2 by 180 degrees so pad1 (now RP_5V) sits at the old pad2 position where the RP_5V track ends and pad2 (now +5V_LOGIC) at the old pad1 position - existing tracks then stay connected. Verify: D2 silk cathode bar faces the U2 / RP_5V side, DRC clean, and D1 (pad1 = VIN, pad2 = GND) unchanged.
VERIFIER: CLAIM STANDS - verified against the actual files.

1. Netlist evidence is real. C:\Users\james\BB8\hardware\netlist\mainboard.py L94 reads exactly: A(two("D2", "SS14", "diode", "Diode_SMD:D_SMA", (0, 48), "+5V_LOGIC", "RP_5V")). two() at L53-55 maps n1 -> pin "1", n2 -> pin "2", so pad1 = +5V_LOGIC, pad2 = RP_5V.

2. Pad1 is physically the cathode. The generated C:\Users\james\BB8\hardware\kicad\mainboard.kicad_pcb (offset ~62433) embeds the real KiCad library D_SMA footprint (version 20260206, generator pcbnew, descr "Diode SMA (DO-..."), not the generator's generic fallback. In it: F.SilkS cathode bar is the line (start -3.51 -1.65)(end -3.51 1.65) at the LEFT end; pad "1" is at (-2 0) with (net 4 "+5V_LOGIC"); pad "2" at (2 0) with (net 53 "RP_5V"). The F.Fab diode glyph has its bar at x=-0.649 and the triangle base at x=0.501 with apex at (-0.649, 0) - i.e. the triangle points toward pad 1, so current flows pad2 -> pad1: pad1 = K, pad2 = A. Therefore K = +5V_LOGIC, A = RP_5V: the diode conducts RP_5V -> +5V_LOGIC and BLOCKS +5V_LOGIC -> RP_5V. The schematic symbol (gen_kicad.py lib_symbol L49-69) is a plain rectangle with passive pins "1"/"2" and no K/A marking, so ERC cannot catch this - the footprint is the only thing that defines polarity.

3. Intent is the opposite. BOM C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv row 15: "D2,1,Schottky,SS14,SMA,Digi-Key,+5V_LOGIC -> RP2350-Zero 5V (USB back-feed block)". Design doc C:\Users\james\BB8\docs\PCB_v10_Design.md L96 "RP2350-Zero 5V via SS14", L103 "RP2350-Zero USB-C (laptop) -. SS14 blocks back-feed .- +5V_LOGIC", L159 "5V | +5V_LOGIC in via SS14 | the Zero has no USB back-feed diode - the SS14 is it". Intended: A = +5V_LOGIC, K = RP_5V. Netlist has it reversed.

4. D1 cross-check confirms the convention is consistent and D2 alone is wrong. PCB offset ~34536, library D_SMB: silk bar at x=-3.66 (left), pad "1" at (-2.15 0) net 67 "VIN", pad "2" at (2.15 0) net 1 "GND". For a unidirectional SMBJ15A TVS, K to the positive rail and A to GND is correct. So pin1 = K is the operative convention; D1 is right, D2 is backwards.

5. Consequence as stated is correct. ZERO_NETS L24 ties the Zero's "5V" pin to RP_5V, and doc L159 states the Zero has no onboard USB back-feed diode (5V pin = VBUS). As built: (a) PS1 +5V_LOGIC cannot reach RP_5V -> RP2350-Zero has no power from the pack -> dome/servo/audio/NeoPixel controller dead in normal operation; (b) a laptop on the Zero's USB-C drives VBUS -> RP_5V -> D2 forward (~0.4-0.5 V drop) -> +5V_LOGIC, back-feeding the DevKit VIN, DFPlayer, U6/U7, U5 LDO, and via F3 the +5V_LED rail. Blocker severity is justified: the board does not function as designed.

6. Recommendation check. Swapping to two("D2", ..., "RP_5V", "+5V_LOGIC") gives pad1 (K) = RP_5V, pad2 (A) = +5V_LOGIC: conducts +5V_LOGIC -> RP_5V, blocks the USB back-feed. Correct and introduces no new electrical problem (SS14 1 A / ~0.45 V drop for the Zero's ~100-200 mA is the intended design). One fab-safety refinement: the recommendation says "regenerate" - the commit log shows the PCB is ~95% routed with manual GUI finishing, so a full regenerate may discard that routing. If the fix is applied to the existing routed board instead, the netlist edit plus a 180-degree rotation of D2 in pcbnew keeps the existing tracks landing on the right pads (pad1 moves from x=-2 to x=+2 where the RP_5V track currently terminates; pad2 moves to x=-2 where the +5V_LOGIC track terminates). Either way, re-check that the silk cathode bar ends up on the RP_5V / U2 side and run DRC.

## [BLOCKER] F2 and F4 BOM MPN 0451003.MRL is a NANO2 (6.1 x 2.7 mm) part but the netlist footprint is 1206 (3.2 x 1.6 mm) — will not fit the pads
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Netlist line 162: F2 fp="Fuse:Fuse_1206_3216Metric"; line 100: F4 fp="Fuse:Fuse_1206_3216Metric". BOM rows F2 and F4: MPN "0451003.MRL", Package "Nano2". Littelfuse 451/453 NANO2 body is 6.10 x 2.69 mm with terminations ~5 mm apart; the 1206 land (gen_kicad.py lines 269-272: two 1.2 x 1.8 mm pads at +/-1.4 mm) cannot take it. The two documents disagree on the physical part, so whichever one the assembler follows is wrong for the other.
REC: Pick one: (A) keep the 1206 footprint and change BOM MPN to a 1206 chip fuse — F2: 0466005.NR (5 A), F4: 0466003.NR (3 A), both Littelfuse 466 series, 1206, fast-acting, >= 32 V rated (pack max 12.6 V); or (B) keep the NANO2 MPNs and change both netlist fp strings to "Fuse:Fuse_Littelfuse_451_453_Nano2" (KiCad library part) and re-run gen_kicad. Option A needs no re-layout.
CORRECTED: Keep the 1206 footprint (no re-layout; the routed mainboard.kicad_pcb already has Fuse_1206_3216Metric at F2 line 306 and F4 line 239) and fix the BOM only: in C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv change line 19 to F2,1,Fuse 3A (charge path),0466003.NR,1206,Digi-Key,... and line 21 to F4,1,Fuse 3A (amp 12V),0466003.NR,1206,Digi-Key,... (Littelfuse 466 series, 3 A, 1206, very fast acting, 32 VDC, 50 A interrupting; the halogen-free suffix 0466003.NRHF is the same part if .NR is unavailable). Keep F2 at 3 A — do NOT change it to 5 A (0466005.NR): netlist line 162 and BOM line 19 and design doc line 371 all say 3 A, and changing a protection rating is a separate decision. Also fix the Package column from "Nano2" to "1206" on both rows so the assembler's BOM agrees with the gerbers. Do NOT take option B as written: the library name "Fuse:Fuse_Littelfuse_451_453_Nano2" does not exist (the KiCad 10 name is "Fuse:Fuse_Littelfuse-NANO2-451_453"), and with a non-matching name gen_kicad.py's generic_footprint() silently falls through to the dummy 2-pin TH footprint at lines 308-311. If NANO2 is ever wanted, use the correct library name, re-run gen_kicad, and re-place/re-route F2 and F4 for the larger ~8 x 4 mm courtyard.
VERIFIER: CLAIM VERIFIED from the files; the mismatch is real and is a fab/assembly blocker as stated.

Evidence check:
- C:\Users\james\BB8\hardware\netlist\mainboard.py line 162: A(two("F2", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", ...,"CHG+","CHG_F")) and line 100: A(two("F4", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", ...,"VIN","AMP_12V_F")). Exactly as cited.
- C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv line 19: F2,1,Fuse 3A (charge path),0451003.MRL,Nano2 and line 21: F4,1,Fuse 3A (amp 12V),0451003.MRL,Nano2. Exactly as cited.
- Pad geometry: the auditor cited gen_kicad.py lines 269-272 (fallback: 1.2 x 1.8 mm pads at +/-1.4 mm). That fallback is NOT what is on the board — gen_kicad.py find_lib_footprint()/embed_lib_footprint() (lines 148-185) embed the installed KiCad 10 library footprint, and the routed C:\Users\james\BB8\hardware\kicad\mainboard.kicad_pcb line 306 (ref F2, nets CHG+/CHG_F) and line 239 (ref F4, nets VIN/AMP_12V_F) both carry "Fuse_1206_3216Metric" with pads (at -1.4 0)/(at 1.4 0) (size 1.25 1.75). So the numbers differ trivially (1.25 x 1.75 vs 1.2 x 1.8) and the conclusion is unchanged.
- NANO2 geometry: installed KiCad 10 library C:\Program Files\KiCad\10.0\share\kicad\footprints\Fuse.pretty\Fuse_Littelfuse-NANO2-451_453.kicad_mod has pads (at -2.455 0)/(at 2.455 0) (size 1.96 3.15). NANO2 pad copper spans 1.475..3.435 mm from center per side; the 1206 pads on the board span 0.775..2.025 mm. Overlap between the NANO2 termination zone and the 1206 pad is at most 2.025-1.475 = 0.55 mm (less once the ~6.1 mm body's actual endcap length is considered) — no viable solder joint, and the 6.1 mm body overhangs a 3.2 mm land by ~1.45 mm per end. Claim of "will not fit" stands.

Recommendation check — partly wrong, two problems:
1. Option A changes F2 from 3 A to 5 A (0466005.NR) with no justification. Netlist line 162 value is "3A fuse", BOM line 19 is "Fuse 3A (charge path)", and PCB_v10_Design.md line 371 says "CHG+ -> 3 A fuse -> directly to pack +". (Doc lines 99/337 say "5 A fuse", so the doc is internally inconsistent, but the netlist+BOM agree on 3 A and the charger is 1 A nominal.) A footprint/MPN fix should not silently change a protection rating; if 5 A is wanted for a 2-3 A charger that must be a separate, explicit decision with the netlist value updated too.
2. Option B uses a library name that does not exist: "Fuse:Fuse_Littelfuse_451_453_Nano2". The installed KiCad 10 library name is "Fuse:Fuse_Littelfuse-NANO2-451_453". With the wrong name, find_lib_footprint() returns None and generic_footprint() falls through every elif ("1206"/"1812" no longer in the string) to the else branch at gen_kicad.py lines 308-311, which silently emits a dummy 2-pin through-hole footprint (1.7 mm circles on 2.54 mm pitch). That would ship a wrong footprint with no error. Option B would also require re-placing F2/F4 (NANO2 courtyard ~8 x 4 mm vs 1206 ~4.4 x 2.4 mm) and re-routing.

466-series rating check for the corrected part: 0466003.NR (3 A, 1206, very fast acting) is rated 32 VAC/32 VDC with 50 A interrupting — adequate for a 12.6 V max pack with a 20 A BMS. Sources: https://canada.newark.com/littelfuse/0466003-nr/fuse-smd-3a-1206-fast-acting/dp/86H2954 , https://www.littelfuse.com/products/fuses-overcurrent-protection/fuses/surface-mount-fuses/thin-film-chip-fuses/466

Confidence: high on the mismatch (read directly from netlist, BOM, routed PCB and installed library). Medium-high on the NANO2 endcap overlap arithmetic (derived from the KiCad library pad dimensions, not the Littelfuse mechanical drawing).

## [BLOCKER] CHG_SENSE cannot detect a charger: CHG+ is permanently tied to pack + through F2/J12, so 'drive locked while charging' is always-true (or never-true)
LENS: TOPOLOGY / CORRECTNESS — charge path vs. common-port 3S BMS pack (J11 -> F2 -> J12, CHG-, CHG_SENSE, J12 connector)
EVIDENCE: Netlist: J11 pin3 -> net CHG+ (mainboard.py L160-161); F2 3A fuse CHG+ -> CHG_F (L162); J12 pin1 '+' = CHG_F (L163-164); R26 47k CHG+ -> CHG_SENSE, R27 10k CHG_SENSE -> GND (L165-166); DEVKIT_NETS VN = CHG_SENSE (L13). There is no series element of any kind between J11.3 and J12.1 other than the fuse (grep: no D3, D4, U8, Q2 in the netlist). Doc §14 L351: the DCZ12-12A ships with ONE pair of bare wires (common-port), so the 'charge lead' into J12 is necessarily a Y-branch of the same XT60 pack leads -> CHG_F == BAT+ off-board, CHG- == pack-minus == board GND (via J1.2). Therefore whenever the pack is plugged in, CHG+ = pack voltage regardless of the charger: CHG_SENSE = V_pack x 10k/(47k+10k) = 12.6 x 0.1754 = 2.21 V (full) ... 9.0 x 0.1754 = 1.58 V (empty) -- identical to VBAT_SENSE (R24/R25 from VIN, L87-88) to within I x R: no-charger difference is only I_board x (R_F1 + R_Q1) ~ 2 A x ~15 mOhm = 30 mV at the node = 5 mV at the ADC; while charging the node rises by I_chg x (R_F2 + R_lead) ~ 1 A x ~50 mOhm = 50 mV = 9 mV at the ADC -- below the ESP32 ADC's uncalibrated error (~1-6 %, 20-130 mV) and the 1 % divider mismatch (22 mV). So no threshold on GPIO39 distinguishes 'charger plugged' from 'not'. Doc §4.2 L134 and §13.3 L337 assume CHG_SENSE = 'charger present'; that assumption is false for this wiring. Side effect of the same topology: the shell charge port contacts are live at pack voltage (fuse-limited to 3 A, dead-short blows F2) at all times. No firmware currently reads GPIO39 (grep of firmware/ for CHG|GPIO39: none), so the fix has no firmware dependency.
REC: Insert the ideal-diode stage the doc already names (§14.2 L371 'LM74610 ideal-diode controller (~0 V drop)') between F2 and J12, and keep R26 on the CHARGER side of it. With the MOSFET off (no charger) the shell-side node CHG+ is isolated from the pack and pulled to ~0 V by R26+R27 (57k); with a charger attached it reads the charger voltage (>= 9 V) -> CHG_SENSE >= 1.58 V. Firmware threshold: charger present if CHG_SENSE > 1.0 V (CHG+ > 5.7 V), absent if < 0.3 V -- an unambiguous 1.3 V gap, and it stays asserted through the CV taper because the charger holds the node. Forward drop is I x Rds(on) (3 A x 10 mOhm = 30 mV; 100 mA end-of-CV = 1 mV) so §14.2's 'CV phase ends 0.4 V short' objection does not apply. Bonus: the shell port is dead until a charger is attached, and a reversed charger is blocked (see reverse-polarity finding). Exact netlist change (mainboard.py, replace L162-164 region):
  A(two("F2", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", (-40, 18), "CHG+", "CHG_F"))                      # unchanged
  A(dict(ref="Q2", value="SQJ402EP N-FET (ideal diode)", kind="ic3", fp="Package_SO:PowerPAK_SO-8_Single", at=(-44, 14), rot=0, side="F",
         pins=[(1,"S","CHG_F"),(2,"D","CHG_PACK"),(3,"G","CHG_GATE")]))   # same S/D/G pin scheme as Q1 (L83-84); body diode anode=S (charger side), cathode=D (pack side)
  A(dict(ref="U8", value="LM74610QDGKRQ1", kind="ic_generic", fp="Package_SO:VSSOP-8_3.0x3.0mm_P0.65mm", at=(-44, 20), rot=0, side="F",
         pins=[ANODE->"CHG_F", CATHODE->"CHG_PACK", GATE_PULL_UP->"CHG_GATE", GATE_PULL_DOWN->"CHG_GATE", VcapH->"U8_VCAPH", VcapL->"U8_VCAPL", NC->"NC"]))  # map pin NUMBERS from the LM74610-Q1 datasheet pin table -- I am not certain of the numbering from memory
  A(two("C29", "1u", "cap", "Capacitor_SMD:C_0603_1608Metric", (-41, 22), "U8_VCAPH", "U8_VCAPL"))
  A(dict(ref="J12", value="Charger XT30", kind="connector", fp="ZCLASS:XT30PW", at=(-46, 8), rot=90, side="F", pins=[(1,"+","CHG_PACK"),(2,"-","CHG-")]))
  R26/R27 unchanged (R26 stays on CHG+, the charger side). NETCLASSES 'Power' pattern "CHG*" already covers CHG_PACK/CHG_GATE (L228). Fallback if you refuse any semiconductor in the path: high-side current sense (INA181A1 + 50 mOhm 2512 between CHG+ and CHG_F, output to VN instead of the divider: 1 A -> 1.0 V, 3 A -> 3.0 V, 100 mA -> 0.1 V); it detects only while current flows, so it drops out at end-of-CV and gives no reverse protection -- the ideal diode is the better fix. Nothing else (sensing before/after the fuse, diode-OR of the divider, comparing CHG_SENSE to VBAT_SENSE) works, because every node on the board's CHG+ side is the same copper as the pack terminal.
CORRECTED: In C:\Users\james\BB8\hardware\netlist\mainboard.py replace L162-166 with (pin NUMBERS match the KiCad footprint pads; nets chosen so R26 stays on the charger side):

A(two("F2", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", (-40, 18), "CHG+", "CHG_F"))                                   # unchanged
# Ideal diode, charger side -> pack side. PowerPAK SO-8 single: pads 1,2,3 = S, 4 = G, 5 = D (tab).
A(dict(ref="Q2", value="SQJ402EP N-FET 40V (ideal diode)", kind="ic3", fp="Package_SO:PowerPAK_SO-8_Single", at=(-44, 14), rot=0, side="F",
       pins=[(1,"S","CHG_F"),(2,"S","CHG_F"),(3,"S","CHG_F"),(4,"G","CHG_GATE"),(5,"D","CHG_PACK")]))
# LM74610-Q1 DGK pinout per SNOSCZ1B p.3: 1 VcapL, 2 GatePullDown, 3 NC, 4 Anode, 5 NC, 6 GateDrive, 7 VcapH, 8 Cathode
A(dict(ref="U8", value="LM74610QDGKRQ1", kind="ic_generic", fp="Package_SO:VSSOP-8_3x3mm_P0.65mm", at=(-37, 25), rot=0, side="F",
       pins=[(1,"VCAPL","U8_VCAPL"),(2,"GPD","CHG_GATE"),(3,"NC","NC"),(4,"ANODE","CHG_F"),(5,"NC","NC"),(6,"GATE","CHG_GATE"),(7,"VCAPH","U8_VCAPH"),(8,"CATHODE","CHG_PACK")]))
A(two("C29", "2.2u X7R 16V", "cap", "Capacitor_SMD:C_0603_1608Metric", (-33, 25), "U8_VCAPH", "U8_VCAPL"))                  # datasheet Fig.13 value; keep away from H1 (-42,24) 6 mm pad
A(dict(ref="J12", value="Charger XT30", kind="connector", fp="ZCLASS:XT30PW", at=(-46, 8), rot=90, side="F", pins=[(1,"+","CHG_PACK"),(2,"-","CHG-")]))
A(two("R26", "47k", "res", "Resistor_SMD:R_0603_1608Metric", (-36, 18), "CHG+", "CHG_SENSE"))                              # unchanged
A(two("R27", "10k", "res", "Resistor_SMD:R_0603_1608Metric", (-36, 21), "CHG_SENSE", "GND"))                                # unchanged
# Bleed for LM74610 reverse leakage (60 uA typ / 110 uA max at -13.5 V, datasheet p.5): 110 uA x 4.34k = 0.48 V at CHG+ -> <= 0.08 V at CHG_SENSE with no charger. 2.7 mA / 34 mW while charging.
A(two("R33", "4.7k 0805", "res", "Resistor_SMD:R_0805_2012Metric", (-36, 15), "CHG+", "GND"))
# ADC clamp already in the BOM (D4 BAT54S, bom L17): pin1 = A1 -> GND, pin2 = A2 -> CHG_SENSE... use the SOT-23 BAT54S series pair: 1 = anode of D1 (GND), 2 = cathode of D2 (+3V3), 3 = common (CHG_SENSE). Protects GPIO39 against a reversed charger pulling CHG+ to -12.6 V.
A(dict(ref="D4", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(-33, 21), rot=0, side="F", pins=[(1,"A1","GND"),(2,"K2","+3V3"),(3,"COM","CHG_SENSE")]))

Firmware (later, no dependency for fab): charger present if CHG_SENSE > 1.0 V (CHG+ > 5.7 V); absent if < 0.3 V. With R33 the idle node is <= 0.08 V, a charger at 9-12.6 V gives 1.58-2.21 V; detection holds through the CV taper because the charger holds the anode node.

Accept knowingly: LM74610 turns the FET off below ~0.2-0.3 A (datasheet Fig.6/7), so the CV tail goes through the body diode (~0.45 V) and the pack terminates ~0.4 V low (~12.2 V, ~90 % SoC). If full charge matters, use LM74700-Q1 (SOT-23-6, regulates ~20 mV down to zero current; verify its pinout and reverse leakage from its datasheet before use) instead of U8.

MANDATORY COMPANION FIX (fab blocker found while checking the recommendation): Q1 L83-84 is mis-pinned on the same footprint - pads 1,2,3 are all Source, so BAT_F, VIN and GND are shorted together in mainboard.kicad_pcb L225 (F1 blows at power-on) and gate/drain float; S/D are also reversed for a P-FET RPP. Replace with:
A(dict(ref="Q1", value="SQJ431EP P-FET RPP", kind="ic3", fp="Package_SO:PowerPAK_SO-8_Single", at=(-1, -26), rot=0, side="F",
       pins=[(1,"S","VIN"),(2,"S","VIN"),(3,"S","VIN"),(4,"G","GND"),(5,"D","BAT_F")]))
Then regenerate (tools/hw/gen_kicad.py), re-run DRC, and re-route the CHG region; verify SQJ402EP is the N-channel 40 V PowerPAK SO-8 part (Vishay SQJ402EP-T1_GE3) on the BOM. NETCLASSES L228 "CHG*" already covers CHG_PACK/CHG_GATE.
VERIFIER: CLAIM VERIFIED from the files. C:\Users\james\BB8\hardware\netlist\mainboard.py L13: "VN":"CHG_SENSE"; L160-161: J11 pin3 = net CHG+, pin4 = CHG-; L162: F2 3A fuse CHG+ -> CHG_F; L163-164: J12 "Charger XT30" pin1 "+" = CHG_F, pin2 "-" = CHG-; L165-166: R26 47k CHG+ -> CHG_SENSE, R27 10k CHG_SENSE -> GND. No D3/D4/U8/Q2 exist in the netlist (only Q1, D1, D2 and U1-U7). docs/PCB_v10_Design.md L134 (GPIO39 "CHG_SENSE (charger present) ... charging -> drive force-disabled"), L337 ("CHG_SENSE -> GPIO39 | drive locked while charging"), L351 (pack output = bare wires, crimp XT60), L371 ("CHG+ -> 3 A fuse -> directly to pack + (common-port BMS - no series diode ...)") all say what the auditor cites. grep of C:\Users\james\BB8\firmware for CHG|GPIO39: no matches. Arithmetic: 10k/(47k+10k) = 0.17544; 12.6 V x 0.17544 = 2.21 V; 9.0 V x 0.17544 = 1.58 V - correct. With J12 wired "directly to pack +" (doc L371) and the pack negative also being board GND via J1.2, CHG+ is the pack terminal whenever the pack is connected, so CHG_SENSE tracks VBAT_SENSE within I*R of F1/Q1/F2/leads (tens of mV at the node, <20 mV at the ADC), below ESP32 ADC offset/nonlinearity and the 1 % divider mismatch (~1.5 % of 2.2 V = 33 mV). The finding stands: no GPIO39 threshold can detect a charger, and the shell port contacts sit at pack voltage. (Residual ambiguity, not enough to refute: if an on-robot CC/CV "charger module" were placed between J12 and the pack, CHG+ would be that module's input and the finding would be moot - but the established facts and doc L371 say J12 goes straight to the pack lead.) Severity note: nothing in firmware reads GPIO39 today, so the board functions without the fix; it is a missing documented feature plus live shell contacts, arguably "major" rather than a fab blocker.

RECOMMENDATION IS WRONG IN FOUR CONCRETE WAYS (so corrected_recommendation is provided):
1) Q2 pin mapping "same S/D/G pin scheme as Q1 (1,S)(2,D)(3,G)" is wrong for Package_SO:PowerPAK_SO-8_Single. The KiCad 10 footprint (C:\Program Files\KiCad\10.0\share\kicad\footprints\Package_SO.pretty\PowerPAK_SO-8_Single.kicad_mod) has pads 1-4 at x=-2.67 (leads) and pad 5 = tab at (0.69,0) 3.81x3.91 plus four leads at x=2.795; Vishay PowerPAK SO-8 single pinout is 1,2,3 = Source, 4 = Gate, 5-8 = Drain. gen_kicad.py embed_lib_footprint (L165-180) assigns nets by netlist pin NUMBER to pad number. As proposed, Q2 would put CHG_F on pad 1 (S) and CHG_PACK on pad 2 (S) - a dead short through the source metallisation, i.e. exactly the topology being complained about - and CHG_GATE on pad 3 (S), shorting the LM74610 gate drive to the anode; gate pad 4 and drain pad 5 float. SIDE FINDING (fab blocker in its own right): Q1 today has the same bug. mainboard.kicad_pcb L225 shows Q1 pad1=BAT_F, pad2=VIN, pad3=GND, pads 4 and 5 with no net. Pads 1-3 are all Source, so BAT_F, VIN and GND are one node: the fused battery + is shorted to GND and F1 blows at power-on; gate and drain are unconnected. Additionally S=BAT_F/D=VIN is backwards for a P-FET RPP (body diode anode is the drain; with source on the battery a reversed pack conducts through the body diode). Must be fixed before fab regardless of this finding.
2) U8 footprint "Package_SO:VSSOP-8_3.0x3.0mm_P0.65mm" does not exist in the KiCad 10 library; the real name is Package_SO:VSSOP-8_3x3mm_P0.65mm. find_lib_footprint returns None for the wrong name and generic_footprint falls through to the "unknown" branch (gen_kicad.py L308-311): eight 1.7 mm through-hole pads on a 2.54 mm pitch - unusable for a VSSOP.
3) U8 pin numbers: LM74610-Q1 datasheet (SNOSCZ1B, p.3, fetched from ti.com) DGK pinout is 1 VcapL, 2 Gate Pull Down, 3 NC, 4 Anode, 5 NC, 6 Gate Drive, 7 VcapH, 8 Cathode. Typical application (p.13, Fig.13) uses a 2.2 uF Vcap capacitor, not 1 uF.
4) Firmware thresholds and the "node pulled to ~0 V by 57k" claim are wrong. Datasheet p.5: I_LKG reverse leakage (anode-cathode = -13.5 V, i.e. exactly the pack-present/no-charger state) = 60 uA typ, 110 uA max over temperature. 60 uA into 57k = 3.4 V (node settles where leakage balances, roughly 2 V at room temperature, up to ~6 V at 110 uA) -> CHG_SENSE idles at ~0.4-1.0 V, so "absent < 0.3 V" is never satisfied and "present > 1.0 V" can be falsely satisfied hot. A low-value bleed resistor on the charger-side node is required (see corrected recommendation).
Also wrong: "100 mA end-of-CV = 1 mV so the 0.4 V short objection does not apply". Datasheet Fig.6/7 (p.6): LM74610 duty cycle falls to 0 % below ~0.2-0.3 A and the charge pump needs >= 0.48 V across the body diode to start (p.5). At the CV tail the FET is off and the pack is fed through the body diode at ~0.45 V drop, so the pack terminates ~0.4 V low (~12.2 V, roughly 90 % SoC) - precisely the §14.2 L371 objection. Safe (undercharge, better cycle life) but it is a capacity trade that must be accepted knowingly; LM74700-Q1 (regulates forward drop to ~20 mV down to zero current) avoids it if full charge matters.
Minor: C29 at (-41,22) sits 2.24 mm from mounting hole H1 at (-42,24), whose MountingHole_3.2mm_M3_Pad pad is 6.0 mm in diameter (gen_kicad.py L306) -> overlap; the collision-aware placer may nudge it but the coordinates should not start inside the hole pad. Reversed-charger exposure: with CHG+ at -12.6 V a reversed charger pulls GPIO39 negative through R26 (~0.2 mA into the ESP32 ESD diode); that exposure exists today too, but the BOM's D4 BAT54S (bom L17, absent from the netlist) should be added to CHG_SENSE.

## [SHOULD-FIX] D2 SS14 is reversed: pad 1 (cathode) is on +5V_LOGIC, so it blocks board->Zero and allows USB->rail back-feed
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist line 94: two("D2","SS14","diode","Diode_SMD:D_SMA",(0,48),"+5V_LOGIC","RP_5V") -> pad 1 = +5V_LOGIC, pad 2 = RP_5V. kicad_pcb: D_SMA library footprint (pad 1 = cathode by KiCad convention; same convention makes D1 on line 85 correct: pad 1 = K = VIN). Doc 4.3: '+5V_LOGIC in via SS14 ... the SS14 is it' (intent: anode on +5V_LOGIC, cathode on RP_5V). As drawn the diode conducts RP_5V -> +5V_LOGIC: the RP2350-Zero gets no 5 V from the board, and a laptop on its USB-C feeds the whole +5V_LOGIC rail (PS1 output, DevKit VIN, DFPlayer). Adjacent-lens finding, reported because it is a one-line fix.
REC: Line 94: two("D2","SS14","diode","Diode_SMD:D_SMA",(0,48),"RP_5V","+5V_LOGIC")  (pin 1 = K = RP_5V, pin 2 = A = +5V_LOGIC).
CORRECTED: C:\Users\james\BB8\hardware\netlist\mainboard.py line 94 -> A(two("D2", "SS14", "diode", "Diode_SMD:D_SMA", (0, 48), "RP_5V", "+5V_LOGIC", rot=180))   # pin 1 = K = RP_5V, pin 2 = A = +5V_LOGIC; rot=180 keeps pad copper at the same x positions as the routed board (RP_5V pad at +2 mm, +5V_LOGIC pad at -2 mm) so only the silkscreen cathode bar flips and no tracks need re-routing. If patching the already-routed kicad_pcb instead of regenerating: rotate D2 180 deg and swap the two pad net assignments (pad 1 -> RP_5V, pad 2 -> +5V_LOGIC); then verify on the fab silkscreen/F.Fab that the cathode bar of D2 is on the RP_5V side (toward U2 / C12), not on the +5V_LOGIC side.
VERIFIER: CONFIRMED from the files. (1) Evidence is real: C:\Users\james\BB8\hardware\netlist\mainboard.py line 94 is exactly two("D2","SS14","diode","Diode_SMD:D_SMA",(0,48),"+5V_LOGIC","RP_5V"), and two() (lines 53-55) maps n1->pin "1", n2->pin "2". (2) Pad 1 really is the cathode: gen_kicad.py embeds the installed KiCad library footprint when present (find_lib_footprint/embed_lib_footprint, lines 148-185), and C:\Program Files\KiCad\10.0\share\kicad\footprints\Diode_SMD.pretty\D_SMA.kicad_mod has its F.SilkS cathode bar at x=-3.51 next to pad 1 (at -2,0) and the F.Fab diode glyph bar at x=-0.649 on the pad-1 side, triangle pointing toward pad 1. The working-tree mainboard.kicad_pcb (fresh unrouted regen, 0 segments) has D2 = library D_SMA with pad 1 -> net 4 "+5V_LOGIC", pad 2 -> net 53 "RP_5V". The routed HEAD version (git show 850cb81, D2 at 166.97,103.01 rot 0) is identical: a 0.3 mm +5V_LOGIC track lands on pad 1 (164.97,103.01) and a 1.2 mm RP_5V track lands on pad 2 (168.97,103.01). So cathode = +5V_LOGIC, anode = RP_5V; the diode only conducts RP_5V -> +5V_LOGIC. (3) Intent is documented: docs/PCB_v10_Design.md line 159 "5V | +5V_LOGIC in via SS14 | the Zero has no USB back-feed diode — the SS14 is it" and line 103 "RP2350-Zero USB-C (laptop) -. SS14 blocks back-feed .- +5V_LOGIC". As built the Zero's 5V pin receives nothing from PS1 (the Zero and everything it drives — servos PWM, dome motor, NeoPixel, DFPlayer UART, amp SHDN — is dead unless a USB cable is attached), and a laptop on the Zero's USB-C forward-biases D2 into +5V_LOGIC, back-feeding PS1 VOUT, DevKit VIN, DFPlayer, U6 and U5 -> +3V3. (4) Consistency check: D1 on line 85 two("D1","SMBJ15A",...,"VIN","GND") gives pad 1 = K = VIN, pad 2 = A = GND, which is the correct TVS orientation under the same pin-1-is-cathode convention, so the convention (and thus the D2 error) is not an artifact of reading the library wrongly. The generated schematic symbol (gen_kicad.py lines 49-69) is a polarity-less rectangle with pins "1"/"2", so nothing in the schematic would have revealed this. (5) Recommendation is electrically correct and safe: with K = RP_5V, A = +5V_LOGIC, the SS14 (40 V / 1 A) drops ~0.35-0.5 V so RP_5V ~4.5-4.65 V, well above the Zero's ME6217 3.3 V LDO dropout; C12 100n stays on RP_5V; V5_SENSE (R29/R30) is on +5V_LOGIC so unaffected; USB on the Zero is blocked since the cathode sits at VBUS. One refinement: add rot=180 so the regenerated pads stay at the same copper positions as the routed HEAD (pad 1 = RP_5V moves to +2,0 where the RP_5V track already lands; pad 2 = +5V_LOGIC to -2,0 where the +5V_LOGIC track lands). Only the silkscreen cathode bar flips to the +x side; no re-routing or DRC change. Without rot=180, applying the fix to the routed HEAD via Update-PCB-from-Schematic leaves the +5V_LOGIC stub on a pad now named RP_5V. Note the severity is understated: this is a functional blocker (half the control system unpowered), not merely should-fix.

## [SHOULD-FIX] C7 1000 uF / 25 V does not exist in the CP_Elec_10x10 can; a 10x10 1000 uF is a 16 V part (79 % at 12.6 V, 90 % at 14.4 V, over-voltage at 19 V)
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist line 102: C7 '1000u/25V' fp Capacitor_SMD:CP_Elec_10x10 (pcb pads 4 x 2.5 mm at +/-4 mm = 10 mm can). BOM row 'C5' (mis-referenced, clearly this cap: 'MAX9744 supply'): EEE-FK1E102P 'SMD 12.5x13.5' - Panasonic FK 1000 uF 25 V is a 12.5 x 13.5 mm can and will not sit on a 10x10 land. The only FK 1000 uF in 10 x 10.2 is the 16 V EEE-FK1C102P. Margins: 12.6/16 = 79 % (marginal vs the usual <= 80 % derating), 14.4/16 = 90 %, 19 V brick = 119 % -> vents. With a true 25 V part: 12.6/25 = 50 %, 19/25 = 76 %, 24.4 V clamp = 98 % (transient only, electrolytic surge rating 1.15-1.25x => OK). C1 is fine: EEE-FK1E221P 220 uF 25 V IS 8 x 10.2 mm, matches CP_Elec_8x10 (line 86).
REC: Line 102: two("C7","1000u/25V","cap","Capacitor_SMD:CP_Elec_12.5x13.5",(62,14),"AMP_12V","GND") and fix the BOM ref C5 -> C7 (MPN EEE-FK1E102P). If board space forbids the bigger can, use two 470 uF 25 V 8x10 (EEE-FK1E471P) in parallel instead of dropping to 16 V.
CORRECTED: Do NOT change the footprint in the netlist or the PCB — leave C7 as Capacitor_SMD:CP_Elec_10x10 (the fab files are correct as they stand; "CP_Elec_12.5x13.5" is not a KiCad library footprint and gen_kicad.py line 274 would silently degrade it to an 8 mm generic land). Fix the BOM instead: in hardware/bom/v10_mainboard_bom.csv line 27 change the ref "C5" -> "C7" and replace EEE-FK1E102P (12.5x13.5, will not fit) with a 10 mm-diameter 1000 uF 25 V part that matches the 4x2.5 mm pads at +/-4 mm, e.g. Nichicon UCD1E102MNL1GS (10x10) or Panasonic EEE-FP1E102AP (10x10.2) — verify diameter/height on the datasheet against the casing clearance before ordering. Keep the netlist value "1000u/25V" (do not drop to the 16 V EEE-FK1C102P). While in the BOM, note lines 27-28 are stale vs the netlist: netlist C5/C6 are the 10u 1206 parts, C7 is the 1000u electrolytic, C8/C9 are 100n 0603 — regenerate the BOM refs from mainboard.py so the assembly house gets a consistent list. Only if a 25 V/10 mm part proves unobtainable should the layout change, and then it must be done in the KiCad GUI (Change Footprint on C7 to CP_Elec_10x12.5 / a custom 12.5 mm land and re-check DRC against J4/J5/J10), not by editing line 102 and regenerating.
VERIFIER: EVIDENCE CONFIRMED. (1) hardware/netlist/mainboard.py line 102 reads exactly: two("C7","1000u/25V","cap","Capacitor_SMD:CP_Elec_10x10",(62,14),"AMP_12V","GND"). (2) hardware/kicad/mainboard.kicad_pcb line 241: C7 is the KiCad library footprint CP_Elec_10x10 (descr "SMD capacitor, aluminum electrolytic, Nichicon, 10.0x10.0mm"), pads "1"/"2" smd roundrect at (-4,0)/(4,0) size 4x2.5, nets AMP_12V (net 6) / GND (net 1) — the claimed pad geometry is accurate. (3) hardware/bom/v10_mainboard_bom.csv line 27: "C5,1,1000uF 25V,EEE-FK1E102P,SMD 12.5x13.5,Digi-Key,MAX9744 supply" — the project's own BOM states the selected 25 V part is a 12.5x13.5 can, and the ref is wrong (netlist C5 on line 95 is a 10u 1206 on +5V_LOGIC). So the core defect — the BOM'd 1000 uF/25 V part cannot sit on the 10x10 land that is in the fab files — is proven from the files. (4) Arithmetic verified: 12.6/16=0.7875 (79%), 14.4/16=0.90, 19/16=1.19, 12.6/25=0.504, 19/25=0.76, 24.4/25=0.976. (5) C1 check: BOM line 23 EEE-FK1E221P "SMD 8x10" vs netlist line 86 CP_Elec_8x10 — consistent, as the claim says.

OVERSTATEMENTS / CAVEATS. (a) "1000 uF/25 V does not exist in a 10x10 can" is only true within the Panasonic FK series the BOM chose; higher-CV series do offer 1000 uF/25 V at 10 mm diameter (e.g. Nichicon UCD1E102MNL1GS 10x10, Panasonic FP EEE-FP1E102AP 10x10.2 — moderate confidence, verify on the datasheet). The KiCad footprint's own descr cites Nichicon. (b) The 14.4 V and 19 V scenarios are not design inputs: the established facts are a 12.6 V max pack and a 12.6 V charger, VIN is TVS-clamped by D1 SMBJ15A, and the MAX9744 itself is only rated to 14 V. In normal operation even a 16 V part sits at 79% — so severity is a BOM/footprint assembly mismatch, not an imminent over-voltage failure. "should-fix" is still reasonable because the part as specified physically will not fit.

RECOMMENDATION IS WRONG AND WOULD INTRODUCE A NEW DEFECT. The suggested footprint "Capacitor_SMD:CP_Elec_12.5x13.5" does not exist in the installed KiCad library (verified listing of Capacitor_SMD.pretty: CP_Elec_10x10, 10x10.5, 10x12.5, 10x12.6, 10x14.3, then 16x17.5 — nothing at 12.5 mm diameter). tools/hw/gen_kicad.py lines 451-457 silently fall back to generic_footprint() when find_lib_footprint() returns None, and line 274 is `d = 10.0 if "10x10" in fp else 8.0` — so the recommended string would synthesize an 8 mm land with 2.5x1.8 mm pads at ±3.5 mm, SMALLER than today, with no warning. Furthermore the PCB has been hand-placed/routed in the GUI (C7 at 212,136; netlist at=(62,14) maps elsewhere; neighbours J4 13.4 mm, J5 14.3 mm, J10 15 mm, H5 16.1 mm centre-to-centre), so editing netlist line 102 alone does not change the fab output — regenerating would discard the ~95% routed board. The parallel-470uF option likewise adds a new part/location to an already-routed board. The safe, zero-copper-change fix is a BOM-only correction.

## [SHOULD-FIX] C4 on the 7.5 V servo rail: netlist says 16 V, BOM lists a 10 V part (EEE-FK1A471P)
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist line 93: C4 '470u/16V' on +7V4_SERVO. BOM row C4: 'EEE-FK1A471P' = Panasonic FK 470 uF 10 V (the '1A' code is 10 V), note still says '+6V_SERVO'. 7.5/10 = 75 % steady; PS2 overshoot at load step puts it at 80-85 %. Not a fire risk, but it is a rating mismatch between the two sources of truth, and the rail was re-rated from 6 V to 7.5 V (doc 13.3) without the BOM following.
REC: BOM C4 MPN -> EEE-FK1C471P (470 uF 16 V, 8 x 10.2 mm, same footprint). No netlist change.
CORRECTED: Edit C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv line 26 from `C4,1,470uF 10V,EEE-FK1A471P,SMD 8x10,Digi-Key,+6V_SERVO` to `C4,1,470uF 16V,EEE-FK1C471P,SMD 8x10,Digi-Key,+7V4_SERVO` (update value text and net note, not just the MPN). Do NOT substitute the 25 V EEE-FK1E471P — it is 10 x 10.2 mm and would not fit the CP_Elec_8x10 footprint. No netlist/Gerber change. Optionally fix the stale '+6V_SERVO' references in docs/PCB_v10_Design.md lines 108-109 to '+7V4_SERVO'.
VERIFIER: Evidence verified against the files. Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py line 93: `A(two("C4", "470u/16V", "cap", "Capacitor_SMD:CP_Elec_8x10", (-20, -20), "+7V4_SERVO", "GND"))` — 16 V, on +7V4_SERVO, exactly as claimed. Netlist line 75 confirms PS2 VOUT drives +7V4_SERVO; BOM row 11 confirms PS2 = Pololu D24V25F7 "7.5V 2.5A"; design doc §13.3 line 334 confirms the rail was re-rated to 7.5 V. BOM C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv line 26: `C4,1,470uF 10V,EEE-FK1A471P,SMD 8x10,Digi-Key,+6V_SERVO` — 10 V part (Panasonic FK '1A' = 10 V code; same MPN as C2 on the 5 V rail, BOM line 24), and the note still says the old +6V_SERVO name. So the BOM/netlist mismatch is real. Arithmetic: 7.5/10 = 0.75 = 75 % — correct. The overshoot figure (80-85 %) is the auditor's estimate and not verifiable from the files, but it does not change the conclusion; even at 75 % steady this is a rating-derating question, not a failure-on-power-up, so 'should-fix' is the right severity. The recommendation is safe for fab: it is BOM-only, the footprint stays CP_Elec_8x10 (gen_kicad.py line 273-275 generates an 8 mm body; EEE-FK1C471P 470 uF 16 V is the 8 x 10.2 mm FK-series size, same as the 10 V part — the 25 V part EEE-FK1E471P would jump to 10 x 10.2 and NOT fit), and no netlist or Gerber change is required. Only refinement: the BOM row should also have its value column and net note updated so the three fields agree, and doc §9 line 108-109 still references '+6V_SERVO' (stale name; cosmetic, not fab-blocking).

## [SHOULD-FIX] ADC dividers (R24/R25, R26/R27) have no clamp: a 19 V source puts 3.33-3.39 V on GPIO35/GPIO39 (abs max 3.6 V), 24 V transient puts 4.28 V
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist lines 87-88 (R24 47k VIN->VBAT_SENSE, R25 10k ->GND -> D35/GPIO35) and 165-166 (R26 47k CHG+->CHG_SENSE, R27 10k -> VN/GPIO39). Ratio 10/57 = 0.1754. 9.0 V -> 1.579 V; 12.6 V -> 2.211 V (matches doc 4.2/14); 14.4 V -> 2.526 V; 19.0 V -> 3.333 V (worst-case 1 % resistors 46.53k/10.1k -> 3.389 V); SMBJ15A VC(max) 24.4 V -> 4.281 V. ESP32 datasheet: IO abs max = 3.6 V; ADC at 11 dB saturates ~3.1 V, linear range 0.15-2.45 V. So 12.6 V and 14.4 V are fine; 19 V is 0.2 V under abs max with no margin and off the ADC scale; 24 V exceeds abs max (injection current limited by the 47k to (24.4-3.6)/47k = 0.44 mA - survivable but out of spec). Note VBAT_SENSE sees the same: with the common-port BMS a wrong charger on J12 also appears on VIN once the BMS opens its charge FET on over-voltage (cells isolated, P+ floats up to the charger voltage). Doc 4.2 row GPIO35 ('BAT54 clamp to 3V3'), doc 4.1 ('+ 3.3 V clamp') and BOM row D4 BAT54S all call for the clamp; the netlist has no D4. Resistor ratings are fine: Yageo RC0603 = 75 V working; P(R26) at 24.4 V = (24.4*47/57)^2/47k = 8.6 mW << 100 mW.
REC: Add one common-cathode dual Schottky clamping both dividers to the board +3V3 rail (U5): A(dict(ref="D4", value="BAT54C", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(44,38), rot=0, side="F", pins=[(1,"A1","VBAT_SENSE"),(2,"A2","CHG_SENSE"),(3,"K","+3V3")])). (BAT54C: pins 1,2 anodes, pin 3 common cathode.) Clamp level ~3.3 + 0.25 = 3.55 V at 0.3 mA; the 47k limits injection to < 0.5 mA in every wrong-charger case. Update BOM D4 MPN to BAT54C.
CORRECTED: In C:\Users\james\BB8\hardware\netlist\mainboard.py, after line 88 (R25) add:
A(dict(ref="D4", value="BAT54C", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(18, 15), rot=0, side="F", pins=[(1,"A1","VBAT_SENSE"),(2,"A2","CHG_SENSE"),(3,"K","+3V3")]))
Placement rationale: put it immediately left of U1 pads 3 (CHG_SENSE, netlist (24.3, 12.7)) and 5 (VBAT_SENSE, (24.3, 7.62)), on the same side of the antenna keepout as the pins, so both sense nets end at the clamp with ~6 mm stubs and only +3V3 needs to be brought in (nearest +3V3 pad: R7 at (18.1, 26)). The placer (gen_kicad.py place_all) will nudge it clear of the axle ring (r>20) and the U1 courtyard; (44,38) as proposed lands across the keepout from the pins and on top of R24's current spot (46.64, 39.76). Pinout is correct as stated: BAT54C pin 1 = A1, pin 2 = A2, pin 3 = common cathode; KiCad SOT-23 pads 1/2 left, 3 right. Then in hardware/bom/v10_mainboard_bom.csv row 17 change Value/MPN from BAT54S to BAT54C (e.g. BAT54C-7-F Diodes Inc or BAT54C,215 Nexperia), keep Package SOT-23 and the "ADC clamps (battery sense / CHG_SENSE)" note. Process: the working-tree mainboard.kicad_pcb is already a fresh unrouted regeneration (0 tracks vs 887 at HEAD), so regenerate with gen_kicad.py, re-autoroute, verify D4 pad 3 reaches net "+3V3" (net 2) and pads 1/2 reach nets 66/16, and regenerate the Gerbers — the mainboard-*.gbr files currently in hardware/kicad (02:59) predate the regeneration and must not be sent to PCBWay. Also note for the finding text: CHG+ has no TVS (D1 is on VIN only, netlist line 85), so the 24 V case on GPIO39 is steady-state 4.21 V, not a transient — the clamp is slightly more important on CHG_SENSE than the claim states.
VERIFIER: Evidence verified against the files. (1) Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py lines 87-88: R24 47k VIN->VBAT_SENSE, R25 10k VBAT_SENSE->GND; lines 165-166: R26 47k CHG+->CHG_SENSE, R27 10k CHG_SENSE->GND; line 13 DEVKIT_NETS maps "VN":"CHG_SENSE" and "D35":"VBAT_SENSE". The regenerated PCB (hardware/kicad/mainboard.kicad_pcb lines 88, 92) confirms U1 pad 3 = CHG_SENSE at netlist (24.3, 12.7) and pad 5 = VBAT_SENSE at (24.3, 7.62). Grep of the netlist and of all 83 PCB footprints: no D3, D4 or U8 exists. (2) Design doc PCB_v10_Design.md line 95 (4.1 power tree: "47k/10k + 3.3 V clamp -> GPIO35"), line 132 (4.2 pin table: "BAT54 clamp to 3V3"), line 336 (13.3: "47k/10k divider + 3.3 V clamp -> GPIO35"); BOM row 17: "D4,1,BAT54S,BAT54S,SOT-23,...,ADC clamps (battery sense / CHG_SENSE)". So the design intent is an explicit clamp on both ADC nodes and the netlist omits it. (3) Arithmetic re-done: 10/57 = 0.17544; 9.0 V -> 1.579 V; 12.6 V -> 2.211 V (matches doc line 347 "2.21 V"); 14.4 -> 2.526; 19.0 -> 3.333 V; 1 % worst case 10.1k/(46.53k+10.1k) = 0.17835 -> 3.389 V; SMBJ15A VC(max) 24.4 V -> 4.281 V; injection (24.4-3.6)/47k = 0.443 mA; P(R26) = (24.4*47/57)^2/47k = 20.12^2/47k = 8.6 mW. All correct. ESP32 IO abs max 3.6 V (VDD+0.3) and 11 dB ADC range are standard datasheet values (not in the repo; cannot be refuted from files). (4) Threat path is real: the pack BMS is common-port, so J12/CHG+ and J1/BAT+ are the same node through the pack harness; a wrong charger appears on both VIN and CHG+. Note the claim actually UNDERSTATES the CHG_SENSE case: D1 SMBJ15A is on VIN only (netlist line 85); there is no TVS on CHG+ at all, so a 24 V brick puts a STEADY-STATE 24*0.1754 = 4.21 V on GPIO39, not just a transient. (5) Recommendation checked: BAT54C is the correct dual for two nodes clamped to one rail (pin 1 A1, pin 2 A2, pin 3 common K, standard across Nexperia/Diodes/Vishay); the existing BOM part BAT54S (series pair) cannot clamp two independent nodes with one package, so the MPN change is right. KiCad footprint Package_TO_SOT_SMD:SOT-23 exists at C:\Program Files\KiCad\10.0\share\kicad\footprints\... with pads 1 (-0.94,-0.95), 2 (-0.94,+0.95), 3 (+0.94,0) — matches the A1/A2/K pin assignment. gen_kicad.py treats kind "ic3" only as a sort key (lines 111, 389); symbols are generated generically, so the dict is accepted; ref "D4" is unused. Clamp level: BAT54 Vf ~0.25-0.3 V at 0.3-0.45 mA -> 3.55-3.6 V: thin against the 3.6 V abs max but a 4.3 V -> 3.6 V improvement and exactly what the doc specifies; leakage (<1 uA at 25 C into the 8.25 k divider impedance) gives <10 mV error, acceptable. (6) Two subtle problems with the recommendation as written, hence corrected_recommendation: (a) placement (44,38) is on the far side of the ESP32 antenna keepout (KEEPOUTS line 222: x 22.5..51.5, y 19..36, F&B.Cu) from the ESP32 pads at x=24.3, and in the current regenerated PCB the placer already put R24 at (46.64, 39.76) — the SOT-23 courtyard (+/-1.93 x +/-1.7) would overlap it; more importantly CHG_SENSE (divider at placer positions (-17,17)/(-23.4,42.6), pin at (24.3,12.7)) would need a ~35 mm stub past the ESP32 pin and around the keepout. The clamp belongs next to U1 pads 3/5. (b) Workflow: gen_kicad.py write_pcb regenerates an unrouted board (docstring lines 9-11, place_all spiral). The working-tree mainboard.kicad_pcb is ALREADY a fresh regeneration (0 segments vs 887 at HEAD 850cb81; Gerbers in hardware/kicad are from 02:59, the PCB from 03:42), so adding D4 via the netlist is the live path right now — but it must be followed by re-autoroute; do not ship the 02:59 Gerbers, which predate any of this.

## [SHOULD-FIX] Q1 gate-source has no zener: VGS = -VIN, which exceeds the SQJ431EP +/-20 V VGS(max) if D1 clamps at 24.4 V
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist line 84: G tied directly to GND, no gate resistor / zener. SQJ431EP: VDS -40 V, VGS +/-20 V. Normal: VGS = -12.6 V (63 % of rating, fine; RDS(on) spec'd at -10 V). 14.4 V charger on the pack: -14.4 V fine. 19 V brick via the BMS-tripped pack: -19 V (95 % of rating). SMBJ15A event (VBR min 16.7 V, VC max 24.4 V at Ipp 24.6 A): -17 to -24 V on the gate oxide. The doc 14.2 scenario (BMS opens at >20 A during a flywheel spin-up -> motor inductance dumps into the bus -> TVS clamps) is exactly the case that puts VGS over 20 V. VDS rating (40 V) is fine in every case.
REC: Add a gate zener + resistor (once Q1 is re-pinned so S = VIN): two("D5","BZT52C12 12V zener","diode","Diode_SMD:D_SOD-123",(-1,-22),"VIN","Q1_G") (pin 1 = K on VIN, pin 2 = A on the gate) and two("R33","47k","res","Resistor_SMD:R_0603_1608Metric",(3,-22),"Q1_G","GND"); change Q1 pin 4 net from GND to Q1_G. VGS is then clamped at -12 V regardless of VIN.
CORRECTED: In C:\Users\james\BB8\hardware\netlist\mainboard.py, replace line 84 with the real SQJ431EP / PowerPAK SO-8 pinout (S1,S2,S3 = pins 1-3, G = pin 4, D = tab = library pad "5"), oriented as a high-side RPP (D on the battery side, S on the load side), and move the gate to a new net:
A(dict(ref="Q1", value="SQJ431EP P-FET RPP", kind="ic3", fp="Package_SO:PowerPAK_SO-8_Single",
       at=(-1, -26), rot=0, side="F", pins=[(1,"S","VIN"),(2,"S","VIN"),(3,"S","VIN"),(4,"G","Q1_G"),(5,"D","BAT_F")]))
Then add, immediately after line 85 (D1):
A(two("D5", "BZT52C12 12V zener", "diode", "Diode_SMD:D_SOD-123", (-1, -22), "VIN", "Q1_G"))   # pin1 = K on VIN (source), pin2 = A on gate
A(two("R33", "47k", "res", "Resistor_SMD:R_0603_1608Metric", (3, -22), "Q1_G", "GND"))
Result: VGS is clamped at ~-12 V for any VIN (12.6 V normal, 14.4 V charger, 19 V brick, 17-24 V SMBJ15A clamp) against the datasheet +/-20 V limit, while still >= the -10 V RDS(on) test condition. Add D5 (BZT52C12, SOD-123) and R33 (47k 0603) to the BOM. Separately and before fab: replace SQJ431EP (200 V, 0.213 ohm, 12 A, PowerPAK SO-8L per Vishay doc 67033) with a -40/-60 V P-FET of RDS(on) <= 10 mohm in a PowerPAK SO-8 land pattern — the gate-zener circuit above is unchanged for any such part.
VERIFIER: EVIDENCE CONFIRMED. C:\Users\james\BB8\hardware\netlist\mainboard.py line 84: Q1 "SQJ431EP P-FET RPP", fp Package_SO:PowerPAK_SO-8_Single, pins=[(1,"S","BAT_F"),(2,"D","VIN"),(3,"G","GND")] — gate tied straight to GND, no series R, no zener. Routed board C:\Users\james\BB8\hardware\kicad\mainboard.kicad_pcb line 225 (library footprint, pads 1-4 + five pads numbered "5" for the drain tab) shows pad1=BAT_F, pad2=VIN, pad3=GND, pad4 and pad5 no net — consistent with the netlist.

RATINGS CONFIRMED FROM THE VISHAY DATASHEET (doc 67033, fetched): VGS abs max = ±20 V (the number the finding depends on); VGS(th) -2.5/-3.0/-3.5 V; RDS(on) spec'd at VGS = -10 V (0.213 ohm max) and -6 V; Ciss 3483-4355 pF; pinout S1, S2, S3, G4, D = tab; package PowerPAK SO-8L. The claim's "VDS -40 V" is wrong — the part is -200 V — but that only makes the "VDS fine in every case" statement more true and does not affect the VGS conclusion.

ARITHMETIC CHECKED: VGS = VG - VS = 0 - VIN. 12.6/20 = 0.63 (63%), 19/20 = 0.95 (95%). SMBJ15A standard datasheet values: VBR 16.7 V min / 18.5 V max, VC 24.4 V at IPP 24.6 A (600 W pulse) — correct. Scenario path: design doc line 39/68 put the DFR0601 motor drivers off-board directly on the pack; doc 14.2 (line 366) describes the BMS opening at >20 A during a flywheel spin-up / drive reversal (= drive-motor regen with nowhere to go); that pumps BAT+, which reaches VIN through Q1's on channel (or, once re-pinned D=BAT_F/S=VIN, through the body diode) and is clamped by D1 on VIN, so VGS = -V(VIN) = -VC. Honest caveat on severity: 24.4 V is the clamp at the full 24.6 A pulse; the energy available (motor L*I^2/2 plus regen into ~2 mF of bus capacitance) realistically clamps at roughly 17-20 V, so this is margin erosion against an absolute-maximum rating rather than a guaranteed violation — "should-fix" is the right tier, and the fix is two cheap parts.

RECOMMENDATION REVIEW: electrically right — BZT52C12 with K on the source (VIN) and A on the gate, plus 47k gate-to-GND, holds |VGS| <= ~12 V for any VIN while still fully enhancing the FET (RDS(on) spec'd at -10 V). KiCad Diode_SMD:D_SOD-123 pad 1 = K, pad 2 = A, the same convention D1 already relies on (D_SMB pad 1 with the cathode bar is on VIN in the PCB file), so two("D5",...,"VIN","Q1_G") has the right orientation. At VIN=12.6 V the zener sits below its knee (leakage <0.1 uA) so the gate is ~0 V, VGS ~ -12.6 V; at a 24 V clamp the gate rises to ~12 V, R33 carries 0.26 mA / 3 mW, VGS ~ -12 V. Gate RC = 47k x 4.4 nF = 0.2 ms, irrelevant for RPP. D5 and R33 are unused in both the netlist and C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv. gen_kicad.py assigns nets purely by pad number (line 165/178), and the schematic symbol generator is pin-count agnostic, so a 5-pin Q1 is fine. No new problem is introduced.

BUT THE RECOMMENDATION IS NOT APPLICABLE AS WRITTEN: it says "change Q1 pin 4 net from GND to Q1_G" — Q1 in the netlist has no pin 4; the gate is pin 3 on line 84. It is only coherent after the adjacent re-pin fix (real part: pins 1,2,3 = Source, 4 = Gate, tab = Drain; today pads 1/2/3 = BAT_F/VIN/GND are all physically Source pins and are shorted inside the die, gate and drain float). So the finding stands, with a corrected, self-contained recommendation below.

ADJACENT BLOCKER FOUND WHILE VERIFYING (out of this lens, must be raised): SQJ431EP is NOT a "30 A P-FET" as BOM lines 12-13 state — it is a 200 V, 0.213 ohm (max at VGS=-10 V), ID -12 A device in PowerPAK SO-8L. As the series RPP element: at 3 A board draw P = 9 x 0.213 = 1.9 W, RthJA 65 C/W (PCB mount) -> +124 C rise; at the 10 A F1 rating P = 21 W. It will cook. The part must be replaced by a -40/-60 V P-FET with RDS(on) <= 10 mohm in the same PowerPAK SO-8 land pattern (verify the chosen MPN's datasheet; note the SO-8L variant also has a longer-lead land pattern than KiCad's PowerPAK_SO-8_Single). The gate zener fix above is still correct for any replacement P-FET with the standard +/-20 V VGS rating.

## [SHOULD-FIX] CHG_SENSE reads the pack voltage at all times, so it cannot signal 'charger present' - only ~50 mV of fuse/harness drop distinguishes charging
LENS: VOLTAGE RATINGS (charge path, VIN, sense dividers, AMP_12V) — with two adjacent-lens blockers found while verifying Q1/D1 polarity
EVIDENCE: netlist line 162-166: R26 is on CHG+, which is the slip-ring side of F2; CHG+ -> F2 -> CHG_F -> J12 -> pack charge lead; with the common-port BMS (established fact) the pack's charge lead IS its main lead, and doc 14.2 explicitly forbids a series diode. So CHG+ = pack voltage (+/- I*R) whenever the pack is plugged in, charger or not. Doc 4.1/4.2 ('CHG_SENSE (charger present) ... charging -> drive force-disabled') assumes a voltage that only exists when a charger is attached. During a 1 A charge the only difference between CHG_SENSE and VBAT_SENSE is I*(R_F2 ~20 mOhm + harness ~30 mOhm) ~ 50 mV at the source = 8.8 mV at the ADC, while a 1 % mismatch between the two dividers alone is 12.6 V * 0.1754 * ~2 % = ~44 mV at the ADC. Not detectable. Voltage-wise the divider is harmless (2.21 V at 12.6 V), so this is functional, not a rating issue.
REC: Keep R26/R27 (they at least tell the firmware the pack lead is live) but do not rely on them for charge lock-out. Options: (1) firmware-only: declare 'charging' when VBAT rises monotonically for > N s or stays > 12.4 V with motors idle; (2) hardware: fit U8 LM74610 (BOM) in the CHG_F path and take CHG_SENSE from the charger side of it (the ideal diode drops only ~20 mV forward, but reverse-blocks, so with no charger CHG+ sits at 0 V and the divider then genuinely reads 'charger present'); this also gives the reverse-charger protection the doc 13.3/14.2 want. Decide before fab because option 2 changes copper.
CORRECTED: Decide before fab (changes copper). Keep R26/R27 on CHG+ (netlist lines 165-166) but do NOT implement the firmware "VBAT > 12.4 V idle" or "monotonic rise" heuristics - the first false-locks a full pack at rest, the second is below ADC resolution at C/12. Hardware fix, per doc 14.2: insert an LM74610 ideal diode between CHG_F and J12 pin 1: netlist line 164 J12 pin 1 becomes a new net (e.g. CHG_PACK); add U8 LM74610QDGKRQ1 (fp Package_SO:VSSOP-8_3.0x3.0mm_P0.65mm) PLUS the two parts the BOM is missing: (a) an N-channel MOSFET >= 30 V, >= 5 A (e.g. Si4410DY, SO-8, or any PowerPAK SO-8 N-FET) with SOURCE on CHG_F (charger/anode side), DRAIN on CHG_PACK (pack/cathode side), body diode pointing charger -> pack; (b) 1 uF 25 V X7R between VCAPH and VCAPL. Wire ANODE -> CHG_F, CATHODE -> CHG_PACK, GATE_DRIVE and GATE_PULL_DOWN -> FET gate (verify pin numbers against the TI datasheet; the part has no GND pin, so CHG- can stay a separate net as it is now). Keep CHG_PACK/CHG_F in the "Power" netclass (1.2 mm track, netlist line 228 already matches "CHG*"). Result: with no charger CHG+ is pulled to ~0 V by the 57k divider (FET reverse leakage ~1 uA x 57k = ~60 mV, ~10 mV at the ADC), with a charger CHG_SENSE = 2.21 V - a clean presence flag on GPIO39; the shell port is no longer live at pack voltage; reverse-plugged charger is blocked. Do not substitute D3 SS54 - doc 14.2 line 371 rejects it (0.3-0.45 V CV shortfall). Also reconcile doc 4.1 line 99 / 13.3 line 337 ("reverse diode -> charger module") with 14.2 so the topology is stated once.
VERIFIER: EVIDENCE VERIFIED against C:\Users\james\BB8\hardware\netlist\mainboard.py: line 161 J11 pin 3 = "CHG+"; line 162 F2 "3A fuse" CHG+ -> CHG_F; line 163-164 J12 "Charger XT30" pin1 = CHG_F, pin2 = CHG-; line 165 R26 47k CHG+ -> CHG_SENSE; line 166 R27 10k CHG_SENSE -> GND; line 13 DEVKIT_NETS "VN":"CHG_SENSE". grep of the netlist confirms no D3, D4 or U8 anywhere. R26 really is on the slip-ring side of F2 as claimed.

TOPOLOGY: Doc 14.2 (PCB_v10_Design.md line 371): "CHG+ -> 3 A fuse -> directly to pack + (common-port BMS - no series diode, or the 12.6 V CV phase ends 0.4 V short)". With a common-port BMS the pack has one P+/P- pair, so the J12 "charge lead" is electrically the same node as the J1 XT60 lead. Hence CHG_F = BAT+ (minus harness I*R) and CHG+ = BAT+ whenever the pack is plugged into J12, charger or not. Doc 4.2 line 134 ("VN | CHG_SENSE (charger present) | divider from CHG+; firmware: charging -> drive force-disabled"), 4.1 line 99 and 13.3 line 337 ("drive locked while charging") all assume a voltage that only appears with a charger attached. With the netlist as built, that assumption is false. Claim stands.
Caveat (only way to escape it): doc 4.1 line 99 and 13.3 line 337 still say "fuse + reverse diode -> charger module" (older wording). If an on-robot CC/CV charger MODULE physically sat between J12 and the pack, CHG+ would be the adapter input and detection would work. But the established facts (J12 -> pack charge lead; external 12.6 V CC/CV charger) and the newer, more specific 14.2 text rule that out. The doc is internally inconsistent on this and should be reconciled, but the claim is correct for the stated topology.

ARITHMETIC CHECKED: ratio 10/(47+10) = 0.17544; 12.6 V x 0.17544 = 2.211 V (ok, and safe on GPIO39 - even a 15 V SMBJ15A clamp level gives 2.63 V < 3.3 V). F2 BOM part is Littelfuse 0451003.MRL (BOM line 19; note BOM says Nano2 while netlist line 162 uses a 1206 footprint - separate issue), cold resistance ~21 mOhm, so 20 mOhm is right; 1 A x (20+30) mOhm = 50 mV; x 0.17544 = 8.8 mV at the ADC (ok). 1 % resistors: ratio sensitivity is R1/(R1+R2) = 0.825 per part, so ~1.65 % per divider worst case, ~3.3 % between two dividers; the reviewer's "~2 %" -> 12.6 x 0.17544 x 0.02 = 44 mV is if anything conservative. Plus ESP32 ADC1 per-channel error after eFuse cal is still tens of mV. Not detectable - confirmed. Stronger than the reviewer stated: the SIGN of (CHG_SENSE - VBAT_SENSE) is positive both while charging (charger lifts CHG+ above BAT+) and while discharging (VIN sags below BAT+ through F1/Q1/harness), so a differential test cannot discriminate even in principle.

RECOMMENDATION CHECK: Keeping R26/R27 is harmless (ok). Option 1 (firmware) is flawed as written: "stays > 12.4 V with motors idle" would false-lock a freshly charged pack at rest (3S Li-ion OCV at 100 % SoC is 12.5-12.6 V for hours); and at 1 A into 12 Ah (C/12) the CC-phase rise is ~0.4 V/h = ~0.2 mV/s at the ADC, below noise and indistinguishable from post-load relaxation. Option 2 (LM74610) is directionally correct and is exactly what doc 14.2 proposes ("LM74610 ideal-diode controller (~0 V drop)"), and it also fixes the fact that the shell charge port is currently live at pack voltage at all times (a short across the shell port dumps the 3S4P pack into F2). BUT it is incomplete: the LM74610 is a CONTROLLER ONLY - the BOM (line 9) lists just the VSSOP-8 and no external N-channel MOSFET or charge-pump capacitor, both of which are mandatory. The "~20 mV" forward drop also ignores the device's ~2 % gate-off duty (body diode conducts ~0.6 V then), giving an average ~25-35 mV at 1 A and ~10 mV at the 100 mA CV tail - still acceptable, negligible CV shortfall. Severity should-fix is fair: a documented safety interlock (drive lock-out while tethered) does not work, and the fix changes copper so it must be decided before PCBWay.

## [SHOULD-FIX] BAT54S clamps (BOM D4) are missing from the netlist; without them GPIO39/GPIO35 exceed the ESP32 3.6 V absolute maximum at CHG+/VIN >= 20.2 V - a steady-state condition when a wrong charger trips the BMS
LENS: ESP32 ADC INPUTS (CHG_SENSE / GPIO39-VN, VBAT_SENSE / GPIO35-D35)
EVIDENCE: Divider ratio R27/(R26+R27) = 10k/57k = 0.17544 (identical for R24/R25, L87-88, L165-166). Node voltage: 9.0 V -> 1.579 V; 12.6 V -> 2.211 V; 14.4 V -> 2.526 V; 19 V -> 3.333 V; 24 V -> 4.211 V. 1 % worst case (10.1k/(46.53k+10.1k) = 0.17835): 12.6 -> 2.247 V, 19 -> 3.389 V, 24 -> 4.280 V; abs-max 3.6 V is reached at 20.2-20.5 V on CHG+/VIN. ESP32 ADC1 ADC_ATTEN_DB_11 (ADC_ATTEN_DB_12 in current IDF) linear/calibrated range 150-2450 mV = 0.86-13.97 V at the pack: the 3S pack (9.0-12.6 V) sits inside it with 1.4 V headroom, 14.4 V (2.53 V) is just above the linear range but safe, 19 V (3.33 V) is saturated and at VDD. GPIO34/35/36/39 are input-only RTC pads: no output driver, no internal pull-up/pull-down (datasheet + §4.2 L119); they do have ESD structures but Espressif does not characterise them as clamps - do not rely on them. Exposure scenarios: (a) CHG+: a 19/24 V supply on the shell port -> BMS over-charge trip at ~4.25 V/cell (12.75 V) opens the charge FET -> CHG+ floats to the charger's full voltage for as long as it is plugged in (hours); because the BMS is common-port this also appears on BAT+ -> F1 -> Q1 -> VIN, so VBAT_SENSE sees the same 4.2 V. (b) VIN transients: D1 SMBJ15A (L85) clamps at 16.7 V (VBR min) to 24.4 V (VC at 24.6 A) -> VBAT_SENSE 2.93-4.28 V for the pulse. Current available is small: Thevenin R = 47k||10k = 8.246k, Vth(24 V) = 4.211 V, I into a pin held at 3.6 V = (4.211-3.6)/8.246k = 74 uA - so damage is unlikely, but it is outside the datasheet. Reverse-plugged charger (-12.6 V on CHG+): node = -2.21 V unclamped; with the clamp, (12.6-0.3)/47k = 0.26 mA through the GND diode - fine. Clamp current with BAT54S to +3V3 at 24 V: node = 3.3 + Vf(BAT54 at ~0.1 mA, 0.24 V typ / 0.32 V max) = 3.54-3.62 V; I(R26) = (24-3.55)/47k = 435 uA; I(R27) = 3.55/10k = 355 uA; diode current = 80 uA (BAT54 rated 200 mA continuous -> 2500x margin); P(R26) = 20.45^2/47k = 8.9 mW (0603 = 100 mW). At 36 V: diode 335 uA, R26 22 mW. The 80 uA pushed into the board +3V3 rail is absorbed by the rail's >= 5 mA of load (hall, AS5600, IMU, pull-ups, DFR0601 VCC) - no rail pumping. Note the DevKit's own 3V3 pin is NC (L16 "3V3":"NC", §4.2 L136), so the clamp references the board AP7361C-33 rail (3.23-3.37 V), not the ESP32's AMS1117 VDD; worst case the node can sit ~0.1 V above VDD+0.3 at ~80 uA, which is benign. BAT54S SOT-23 pinout (Nexperia/Diodes/onsemi agree): pin1 = A1 (anode of diode 1), pin2 = K2 (cathode of diode 2), pin3 = K1/A2 (centre tap); diodes point pin1 -> pin3 -> pin2. KiCad Package_TO_SOT_SMD:SOT-23 is installed (C:\Program Files\KiCad\10.0\share\kicad\footprints\Package_TO_SOT_SMD.pretty\SOT-23.kicad_mod; pads 1,2 on one side, 3 opposite) and the generator embeds library footprints (gen_kicad.py L148-156, symbols are generic per pin list L39-69 so kind="ic3" works). One BAT54S clamps ONE node; BOM row 17 lists qty 1 for two nodes.
REC: Add after L166:
A(dict(ref="D4", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(41, 38), rot=0, side="F",
       pins=[(1,"A1","GND"),(2,"K2","+3V3"),(3,"K1A2","VBAT_SENSE")]))
A(dict(ref="D5", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(-33, 24), rot=0, side="F",
       pins=[(1,"A1","GND"),(2,"K2","+3V3"),(3,"K1A2","CHG_SENSE")]))
(pin3 = sense node clamps to GND-0.3 V .. +3V3+0.3 V). BOM row 17: D4 -> 'D4-D5', qty 2. Single-part alternative if you only want the over-voltage clamp: one BAT54C (common cathode) pins=[(1,"A1","VBAT_SENSE"),(2,"A2","CHG_SENSE"),(3,"K","+3V3")]. Place each SOT-23 next to its 10k bottom resistor (R25 / R27); the generator's spiral placement will move them if they collide - check in the GUI. Footprint-free fallback if you refuse the parts: change R27 (and R25) to 6.8k -> ratio 0.1264: 12.6 V -> 1.59 V, 24 V -> 3.03 V (< 3.3 V, no clamp needed up to 28.5 V), resolution 6.4 mV/LSB of pack instead of 4.6 mV - but that does not cover the 30 V+ or transient cases the clamp does.
CORRECTED: Keep the two BAT54S parts exactly as proposed (pinout and footprint verified): add after mainboard.py L166
A(dict(ref="D4", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(47, 41), rot=0, side="F", pins=[(1,"A1","GND"),(2,"K2","+3V3"),(3,"K1A2","VBAT_SENSE")]))
A(dict(ref="D5", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(-33, 21), rot=0, side="F", pins=[(1,"A1","GND"),(2,"K2","+3V3"),(3,"K1A2","CHG_SENSE")]))
(D4 placed beside R25 (44,41) on the side away from the antenna keep-out (KEEPOUTS L222, y<=36); D5 beside R27 (-36,21). The 2.5 mm-gap placer may still nudge them - verify in the GUI that no SOT-23 pad lands inside the antenna rule area.) BOM row 17: Ref "D4-D5", Qty 2.

Correct the finding's severity rationale: D1 SMBJ15A bounds CHG+/VIN (same node via F2/pack/F1/Q1) to 16.7-19.3 V, so the wrong-charger case gives <= 3.44 V at the pins while D1 lives (then D1 fails short and F1 blows); the clamps protect against transients at D1's VC (up to 24.4 V -> 4.28 V unclamped), D1 open/absent, charger present with pack unplugged (2.2 V into an unpowered ESP32), and a future 4S pack. Worst-case clamped node is 3.366 V (AP7361C +2%) + 0.32 V (BAT54 Vf max) = 3.69 V at ~80 uA - still marginally above 3.6 V but benign at that current.

Process (mandatory before fab): the netlist edit does not modify mainboard.kicad_pcb. Re-run tools/hw/gen_kicad.py (this regenerates the PCB and discards the current ~95% routing), then tools/hw/route.py, tools/hw/finish.py, finish the remaining opens in the GUI, rerun DRC, and regenerate the gerbers with tools/hw/fab_outputs.ps1 - do NOT ship the currently untracked hardware/kicad/*.gbr set. Alternatively add D4/D5 by hand in the KiCad schematic + routed PCB and make the same netlist edit for the record, so the source of truth does not diverge. Optional on the same net: change the NETCLASSES L228 "CHG*" glob to explicit "CHG+", "CHG_F", "CHG-" so CHG_SENSE routes as Default (0.3 mm) rather than a 1.2 mm Power track.

Separate fab-blocking finding to raise (out of this lens): Q1 L83-84 pin map 1=S/2=D/3=G does not match Package_SO:PowerPAK_SO-8_Single pads (1-3 = S, 4 = G, 5 = D): BAT_F, VIN and GND are all on the bonded source pins (dead short), gate/drain unconnected, and S/D are inverted for a high-side P-FET reverse blocker. Must be pins=[(1,"S","VIN"),(2,"S","VIN"),(3,"S","VIN"),(4,"G","GND"),(5,"D","BAT_F")] (battery on drain, load on source, gate to GND).
VERIFIER: EVIDENCE IS REAL. hardware/netlist/mainboard.py L13 maps "VN"->CHG_SENSE and "D35"->VBAT_SENSE; L16 "3V3":"NC"; L85 D1 SMBJ15A VIN-GND; L87-88 R24 47k VIN->VBAT_SENSE, R25 10k ->GND; L165-166 R26 47k CHG+->CHG_SENSE, R27 10k ->GND. No D3, D4 or U8 exists anywhere in the netlist (full file read, 233 lines). BOM row 17: "D4,1,BAT54S,BAT54S,SOT-23,Digi-Key,ADC clamps (battery sense / CHG_SENSE)". Design doc L95 "47k/10k + 3.3 V clamp -> GPIO35", L132 "47k/10k divider (12.6 V -> 2.21 V), BAT54 clamp to 3V3", L336 same. So the BOM+design intent vs netlist discrepancy is confirmed.

ARITHMETIC CHECKS OUT. 10k/57k = 0.175439: 9.0->1.579, 12.6->2.211, 14.4->2.526, 19->3.333, 24->4.211 V. 1% worst 10.1k/56.63k = 0.17835: 12.6->2.247, 19->3.389, 24->4.280. 3.6 V abs-max reached at 3.6/0.17835 = 20.19 V (worst) / 3.6/0.17544 = 20.52 V (nominal). Thevenin 47k||10k = 8.246k; (4.211-3.6)/8246 = 74 uA. Clamp at 24 V: I(R26) = (24-3.55)/47k = 435 uA, I(R27) = 355 uA, diode 80 uA; P(R26) = 20.45^2/47k = 8.9 mW; at 36 V 32.45^2/47k = 22.4 mW, diode 335 uA. All correct. ESP32 ADC 11 dB range 150-2450 mV -> 0.855-13.97 V at the pack: correct.

FOOTPRINT/PINOUT CORRECT. C:\Program Files\KiCad\10.0\share\kicad\footprints\Package_TO_SOT_SMD.pretty\SOT-23.kicad_mod exists: pads 1 and 2 at x=-0.9375 (y=-0.95/+0.95), pad 3 at x=+0.9375. BAT54S (Nexperia/Diodes/onsemi): pin1 = A1, pin2 = K2, pin3 = K1/A2, so pin3 = protected node, D1 GND->node, D2 node->+3V3: node held to GND-Vf..+3V3+Vf. gen_kicad.py accepts kind="ic3" (order dicts L111 and L389) and embeds library footprints via find_lib_footprint/embed_lib_footprint (L148-185); pin names like "K1A2" are just symbol text. Ref D5 collides with nothing (netlist has only D1, D2). BAT54C alternative pinout (1=A1, 2=A2, 3=K) is also correct.

WHERE THE CLAIM OVERREACHES (severity framing, not the finding). The "steady-state for hours at 20-24 V" scenario ignores that D1 is on the same node: CHG+ -> F2 -> J12 -> pack P+ = B+ -> J1 BAT+ -> F1 -> Q1 (on, Vgs = -Vbat) -> VIN -> D1. Everything in that path is tens of milliohms, so D1 bounds CHG+/VIN to VBR 16.7-18.5 V (about 19.3 V worst at 3 A with ~0.28 ohm dynamic impedance) -> sense node <= 19.3 x 0.17835 = 3.44 V, i.e. UNDER the 3.6 V abs max while D1 is intact. A 1-3 A CC charger into D1 at ~18 V is 18-55 W, so D1 fails (normally short) within seconds, which discharges the cells through the short until F1 (10 A) blows; the outcome is a dead board, not a sustained 24 V on the ADC pins. The cases the clamp genuinely covers are: (1) surge transients where D1 sits at VC up to 24.4 V -> 4.28 V unclamped vs ~3.6-3.7 V clamped; (2) D1 failed-open or unpopulated; (3) charger plugged into the shell port with the pack disconnected (J1 open): CHG_SENSE = 2.2 V driven into an UNPOWERED ESP32 (board +3V3 and ESP VDD both 0 V) - the clamp diverts that into the 3V3 rail at 0.26 mA; (4) a future pack change. Also note the worst-case stack WITH the clamp: AP7361C-33 +2% = 3.366 V + BAT54 Vf max 0.32 V at 0.1 mA = 3.69 V at 80 uA - about 0.1 V over the 3.6 V abs max, benign at that current but not a guarantee; the auditor's "3.54-3.62 V" uses typical Vf. And BAT54 reverse leakage (2 uA max at 25 V/25 C, far less at ~1-2 V reverse bias) across the 8.25k Thevenin adds <1 mV at room temp, a few mV at 85 C - acceptable for a battery gauge. Severity "should-fix" is still right: doc and BOM both specify the part, the netlist omits it, and the mitigation is two SOT-23s.

RECOMMENDATION IS ELECTRICALLY CORRECT BUT MISSES A PROCESS HAZARD for a board going to fab: editing mainboard.py does NOT touch hardware/kicad/mainboard.kicad_pcb. gen_kicad.py write_pcb (L426 onward) regenerates the PCB from scratch with place_all spiral placement (L367-414), so the ~95% routing from commits bf32721/850cb81 is discarded and the whole gen -> tools/hw/route.py (Freerouting) -> tools/hw/finish.py -> GUI-finish of remaining opens cycle must be rerun, and the untracked hardware/kicad/*.gbr files regenerated via tools/hw/fab_outputs.ps1. Placement coords given ((41,38) and (-33,24)) will be shuffled by the 2.5 mm-gap placer anyway (R25 at (44,41) is within the gap); (41,38) also sits 2 mm above the antenna copper keep-out (KEEPOUTS L222: y 19-36) which place_all does not check - DRC will catch it if the placer lands pads inside. Side issue on the same net: NETCLASSES L228 glob "CHG*" captures CHG_SENSE into the Power class (1.2 mm track, 1.0 mm via) - a routing nuisance for a 47k divider node to a DevKit header pin.

OUT-OF-SCOPE BUT FAB-BLOCKING, found while tracing the D1 path (confidence high): mainboard.py L83-84 maps Q1 pins 1=S BAT_F, 2=D VIN, 3=G GND onto Package_SO:PowerPAK_SO-8_Single, whose pads are 1,2,3 = Source, 4 = Gate, 5 = Drain tab (KiCad footprint verified; Vishay SQJ431EP pinout 1-3 S, 4 G, 5-8 D). As wired, BAT_F, VIN and GND all land on the FET's three internally bonded source leads (BAT_F shorted to GND inside the package -> F1 blows at first power-up), and the gate and drain pads have no net. Separately, even with correct pad numbers the S/D assignment is inverted for a high-side P-FET reverse-polarity blocker (battery should be on the drain; with battery on the source the body diode D->S conducts under reverse polarity). This needs its own finding.

## [SHOULD-FIX] No capacitor on either sense node: 8.25 k source impedance into the SAR ADC with 57-75 mm of high-impedance trace; add 100 nF per node (Espressif's own recommendation) - negligible detect latency
LENS: ESP32 ADC INPUTS (CHG_SENSE / GPIO39-VN, VBAT_SENSE / GPIO35-D35)
EVIDENCE: Source impedance seen by the ADC = R26||R27 = 47k*10k/57k = 8.246 k (same for R24||R25). ESP32 does not publish a max source impedance; the ESP-IDF ADC guide ('Minimizing noise') explicitly recommends a ~100 nF ceramic on the ADC pad plus multisampling, and the board already does exactly this for the S2S pot input (L133-134: R2 1k + C28 100n). Routed HEAD PCB: VBAT_SENSE 17 segments, 57.1 mm total on F.Cu/In2.Cu/B.Cu; CHG_SENSE 10 segments, 74.5 mm on F.Cu/In2.Cu (In2 = the +5V plane layer), and R26 (132.97,133.0) and R27 (126.64,107.43) are 26 mm apart so the high-Z node itself spans that distance past U2 (140,117) and J_AMP (119,117) carrying class-D speaker outputs. With a 100 nF on the node the HF impedance to GND is 80 ohm at 20 kHz (motor PWM) and 0.5 ohm at 300 kHz (class-D carrier), independent of where on the net the cap sits for kHz-range capacitive pickup. RC: tau = 8.246k x 100 nF = 0.825 ms, fc = 1/(2*pi*8.246k*100n) = 193 Hz, 99 % settle in 3.8 ms. Charger-present detect latency is therefore set by firmware debounce (should be >= 100s of ms anyway for slip-ring bounce), not by the cap. It also absorbs the TVS-clamp transient case in the clamp finding (a 100 us event reaches ~12 % of its step at the node). Leakage/bias: ESP32 input leakage ~50 nA x 8.246k = 0.4 mV - negligible.
REC: Add: A(two("C29", "100n", "cap", "Capacitor_SMD:C_0603_1608Metric", (41, 41), "VBAT_SENSE", "GND")) and A(two("C30", "100n", "cap", "Capacitor_SMD:C_0603_1608Metric", (-33, 21), "CHG_SENSE", "GND")). BOM: covered by the C10-C25 100 nF pool (only C8-C12 are used in the netlist), add 'C29-C30' to that row. Move R26's 'at' next to R27 (e.g. (-36, 18) and (-36, 21) are already adjacent in the netlist; the collision-avoidance placement separated them - pin them together in the GUI so the node is short). Firmware: average 32-64 samples per reading.
CORRECTED: 1) Netlist (hardware/netlist/mainboard.py), after L88 and after L166 respectively - same parts as proposed (using pool refs avoids a BOM edit; C29/C30 also fine if 'C29-C30' is added to BOM L29):
   A(two("C13", "100n", "cap", "Capacitor_SMD:C_0603_1608Metric", (44, 37), "VBAT_SENSE", "GND"))
   A(two("C14", "100n", "cap", "Capacitor_SMD:C_0603_1608Metric", (-36, 26), "CHG_SENSE", "GND"))
   Hints must be >=4 mm apart in y (or >=5.5 mm in x) from their neighbours or place_all (gen_kicad.py L385, gap=2.5 mm) will spiral them away - e.g. R24 (44,44) / R25 (44,40) / C13 (44,36); R26 (-36,18) is inside PS1's courtyard+gap and gets moved anyway, so do not rely on hints there.
2) Narrow NETCLASSES L228 from "CHG*" to "CHG+", "CHG-", "CHG_F" so CHG_SENSE routes in Default (0.3 mm) instead of Power (1.2 mm) - all 10 HEAD CHG_SENSE segments are currently 1.2 mm.
3) After regenerating and BEFORE autorouting, in the KiCad GUI drag R25 + C13 to within ~5 mm of U1 pad 5 (D35, module-local (-12.7,-7.62)) and R27 + C14 to within ~5 mm of U1 pad 3 (VN, module-local (-12.7,-12.7)), cap GND pad on a via to the In1 GND plane; the 47k top resistors can stay wherever the placer puts them - the long run must be on the low-impedance VIN / CHG+ side of the 47k, not on the 8.25 k node. Lock those four 0603s before running Freerouting.
4) Re-plot gerbers after the re-route: the mainboard-*.gbr files on disk (2026-08-22 02:59) were plotted from the pre-regeneration board and do not contain these parts (or the J4/J5/J8 netlist changes already in the working copy).
5) Firmware: average 32-64 raw samples (or use the ESP-IDF calibrated multisample) per reading; keep charger-present debounce >=100 ms.
Correction to the evidence text: 100 nF is 5.3 ohm at 300 kHz (not 0.5 ohm); VBAT_SENSE HEAD length is 57.3 mm.
VERIFIER: VERIFIED against the files (not refuted).

Netlist (C:\Users\james\BB8\hardware\netlist\mainboard.py, working copy; sense parts unchanged vs HEAD):
- L87 R24 47k VIN->VBAT_SENSE at (44,44); L88 R25 10k VBAT_SENSE->GND at (44,41). L165 R26 47k CHG+->CHG_SENSE at (-36,18); L166 R27 10k CHG_SENSE->GND at (-36,21). L13: VN->CHG_SENSE, D35->VBAT_SENSE. Grep of the whole file: VBAT_SENSE appears only at L13/L87/L88 and CHG_SENSE only at L13/L165/L166 - there is NO capacitor on either node. Claim confirmed.
- L133-134: R2 1k S2S_OUT->S2S_POS + C28 100n S2S_POS->GND - the board does exactly the recommended RC on the third ADC input (and docs/PCB_v10_Design.md L131 specifies it for D34, while L132/L134 specify no cap for D35/VN). Claim confirmed.
- C13-C25 are unused in the netlist; 100n refs used are C8-C12 and C28. BOM L29 'C10-C25,16,100nF 50V X7R,CL10B104KB8NNNC,0603' pool exists. Claim confirmed.

Routed PCB: the WORKING-TREE mainboard.kicad_pcb was regenerated at 03:42 (generator single-line format, 0 segments, 2 vias), so the auditor's numbers can only refer to HEAD (850cb81), which I parsed: 887 segments / 110 vias total. VBAT_SENSE: 17 segments, 57.3 mm (F.Cu 18.4 / B.Cu 19.1 / In2.Cu 19.8), 2 vias - auditor said 57.1, trivial rounding. CHG_SENSE: 10 segments, 74.5 mm (F.Cu 56.0 / In2.Cu 18.5), 1 via - exact match. R26 at (132.97,133.0), R27 at (126.64,107.43): 26.3 mm apart - confirmed; U2 at (140,117), J_AMP at (119.45,116.92) - confirmed; the 24.6 mm F.Cu run at y=128.86 skirts U2's lower edge and the x=130.1 run is ~2.7 mm from J_AMP's courtyard. Extra fact the auditor missed: every CHG_SENSE segment is 1.2 mm wide because NETCLASSES L228 'Power' pattern 'CHG*' globs CHG_SENSE - a 1.2 mm x 74 mm high-Z trace has ~4x the pickup area of a 0.3 mm one.

Arithmetic: R26||R27 = 47k*10k/57k = 8245.6 ohm (ok). tau = 8245.6 x 100 nF = 0.825 ms (ok). fc = 1/(2*pi*0.825 ms) = 193 Hz (ok). 99% settle = 4.6 tau = 3.8 ms (ok). Xc(100 nF, 20 kHz) = 1/(2*pi*2e4*1e-7) = 79.6 ohm (ok). Xc at 300 kHz = 1/(2*pi*3e5*1e-7) = 5.3 ohm, NOT 0.5 ohm - auditor is off by 10x, but 5.3 ohm vs 8.25 k source is still ~64 dB attenuation, conclusion unchanged. TVS transient: 1-exp(-100 us/825 us) = 11.4% (~12%, ok). Leakage 50 nA x 8.25k = 0.41 mV (ok). 20 kHz motor PWM per design doc L125 (ok).

Espressif reference: ESP-IDF ADC driver docs, 'Minimize Noise' section, do recommend a bypass capacitor (e.g. 100 nF ceramic) on the ADC input pad plus multisampling - consistent with my knowledge; moderate-high confidence (not fetched).

Safety of the recommendation: two() signature (ref,value,kind,fp,at,n1,n2) matches L53-55; footprint and kind are the ones already used by C28; nets exist; GPIO35/39 are input-only with no pulls so a 100 nF on an 8.25 k node changes nothing at DC, adds 0.24 uJ max stored energy, no stability concern (no op-amp), ADC1 so no Wi-Fi conflict. No new problem introduced.

What IS wrong/weak in the recommendation (hence corrected_recommendation): (1) the 'at' hints are not honored - gen_kicad.py place_all (L367-414) spiral-searches with gap=2.5 mm, so two 0603s at 3 mm pitch always collide and one is moved; in the regenerated working PCB R26 landed at netlist-frame (-17.0,17.0), R27 at (-23.4,42.6), F2 at (-29.8,39.7), and J12 85 mm from its hint - so '(-36,18)/(-36,21) are already adjacent' is meaningless and C30 at (-33,21) will be flung too. (2) 'pin them in the GUI' is moot until after the next regeneration, because the working PCB is unrouted; the fix must be in the netlist before re-route. (3) The on-disk gerbers (02:59) predate the regen (03:42) and are stale vs the working netlist - whichever way this lands, gerbers must be re-plotted. (4) The 'CHG*' netclass glob should be narrowed so CHG_SENSE is not routed as a 1.2 mm Power trace.

## [SHOULD-FIX] Battery-gauge accuracy: uncalibrated ESP32 ADC error can consume the whole 0.6 V margin between the 9.6 V firmware cutoff and the 9.0 V BMS cutoff
LENS: ESP32 ADC INPUTS (CHG_SENSE / GPIO39-VN, VBAT_SENSE / GPIO35-D35)
EVIDENCE: Scale: 12-bit, ~0.806 mV/LSB at the node -> 0.806/0.17544 = 4.6 mV of pack per LSB (fine). Error budget at 9.6 V (node 1.684 V): 1 % resistors -> +/-1.4 % ratio -> +/-0.13 V; ESP32 internal Vref spread 1000-1200 mV (+/-6 %) uncalibrated -> +/-0.58 V; with eFuse Vref/two-point calibration (adc_cali / esp_adc_cal) +/-~2 % -> +/-0.2 V; ADC INL in the 150-2450 mV range adds ~+/-10 LSB = +/-46 mV of pack. §14 L354: warn 10.2 V, force-disable drive at 9.6 V, BMS cuts at 9.0 V (§14.1). Uncalibrated, the 9.6 V trip can land anywhere in 9.0-10.2 V - i.e. either the BMS drops the droid first or it stops at >60 % of the usable range. Also the 2.21 V full-pack reading sits at the upper third of the linear range where ESP32 INL is worst; keep the 47k/10k (do not lower the ratio unless you also accept coarser resolution).
REC: No netlist change. Firmware: configure ADC1_CH7 (GPIO35) / ADC1_CH3 (GPIO39) with ADC_ATTEN_DB_11(12), 12-bit, use esp_adc_cal_characterize / adc_cali with eFuse Vref, multisample 64, and add a one-point scale calibration against a multimeter reading of the pack (e.g. 'pref vbatcal <mV>') stored in NVS; recheck the 9.6 V cutoff after calibration. Read VBAT only when motor PWM duty is low or take the median of a burst to reject sag.
CORRECTED: No mandatory netlist change. Firmware (ESP32_DRIVE_RC4, new in v10): analogReadResolution(12); analogSetPinAttenuation(35, ADC_11db) and (39, ADC_11db); read with analogReadMilliVolts(35)/analogReadMilliVolts(39) (Arduino-ESP32 core applies eFuse-Vref calibration via esp_adc_cal/adc_cali, bringing the +/-9 % chip-to-chip Vref spread down to ~+/-2 %), do NOT use raw/4095*3.3 as ESP32_DOME_RC4.ino L368 does; multisample 64 (or median of a burst taken when drive/flywheel PWM is low); pack mV = node_mV * 5.7 * k, with k a one-point scale calibration against a multimeter (`pref vbatcal <mV>` stored in NVS) which also removes the +/-1.65 % worst-case error of the 1 % 47k/10k pair; re-verify the 9.6 V `lowbat` trip after calibration (target: trips within +/-0.1 V of 9.6 V, comfortably above the 9.0 V BMS cutoff). Optional and fab-safe: add a 100 nF 0603 across R25 (VBAT_SENSE-GND) and R27 (CHG_SENSE-GND) for 0.8 ms RC ripple filtering; or specify R24-R27 as 0.1 % parts in the BOM (same footprint). Keep the 47k/10k ratio.
VERIFIER: Evidence verified against the files. Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py: L87 R24 47k VIN->VBAT_SENSE, L88 R25 10k VBAT_SENSE->GND, L13 "D35":"VBAT_SENSE" and "VN":"CHG_SENSE"; L165/166 R26 47k / R27 10k for CHG_SENSE. No capacitor exists on VBAT_SENSE or CHG_SENSE (only R24/R25/R26/R27 touch those nets). Design doc C:\Users\james\BB8\docs\PCB_v10_Design.md L354: warn 10.2 V, force-disable drive at 9.6 V, `pref lowbat 9.6`; L360: Li-ion 3S full/empty 12.6/9.0 V; L336 and L347: "47k/10k -> 2.21 V at 12.6 V". All citations are real.

Arithmetic check: ratio 10/57 = 0.17544; 12.6 V -> 2.211 V (matches doc), 9.6 V -> 1.684 V; 3.3 V/4096 = 0.806 mV/LSB -> 0.806/0.17544 = 4.59 mV of pack per LSB. All correct. Two error terms are slightly understated by the auditor, which only strengthens the conclusion: (a) 1 % resistors give a worst-case ratio error of (1-0.17544)*2 % = +/-1.65 % (e.g. 10.1k/(46.53k+10.1k)=0.17835), i.e. +/-0.16 V at 9.6 V, not +/-1.4 %/0.13 V; (b) Espressif specifies the ESP32 ADC reference as 1100 mV nominal with a 1000-1200 mV chip-to-chip range, i.e. +/-9 %, not +/-6 %, so the uncalibrated error at 9.6 V is up to +/-0.87 V, not +/-0.58 V. Datasheet INL is +/-12 LSB (auditor's ~10 LSB is fine). Either way the uncalibrated error exceeds the 0.6 V margin between the 9.6 V firmware cutoff and the 9.0 V BMS cutoff, so the core claim stands.

Firmware state confirms the risk is not hypothetical: ESP32_DRIVE_RC4 has no VBAT/lowbat code at all yet (grep for lowbat/VBAT_SENSE/GPIO35 returns nothing), and the sibling dome firmware (C:\Users\james\BB8\firmware\ESP32_DOME_RC4\ESP32_DOME_RC4.ino L366-369) uses the uncalibrated pattern `raw/4095.0*3.3*2.0`, so the same mistake is likely to be copied unless called out.

The recommendation (no netlist change, calibrate in firmware) is correct and fab-safe. Minor corrections: in the Arduino-ESP32 core the simplest correct call is analogReadMilliVolts(35)/(39), which already applies eFuse-Vref calibration via esp_adc_cal/adc_cali, so it should be named; ADC_ATTEN_DB_11 vs DB_12 are the same setting (alias renamed in newer IDF). One optional, harmless board tweak exists: a 100 nF 0603 across R25 (VBAT_SENSE->GND) and R27 (CHG_SENSE->GND) gives RC = 8.2k*100n = 0.8 ms of PWM-ripple filtering at a Thevenin source impedance of 8.2k; it is not required if the median/multisample is done in firmware. Severity as a fab gate is really "firmware note" since nothing on the PCB changes, but the finding is valid.

## [SHOULD-FIX] F2 = 3 A with a 3 A charger is 100 % fuse loading; doc says 5 A in two places and 3 A in one — use 5 A
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Netlist line 162: A(two("F2", "3A fuse", ... "CHG+", "CHG_F")). BOM row F2: "Fuse 3A (charge path)". Doc §4.1 line 99: "CHG+ -> 5 A fuse + reverse diode"; §13.3 line 337: "5 A fuse + SS54"; §14.2 line 371: "CHG+ -> 3 A fuse -> directly to pack +". Charger: §14 line 349 "12.6 V 1 A ... a 2-3 A charger halves the time". Derating arithmetic: 1 A / 3 A = 33 % (fine); 3 A / 3 A = 100 %. Chip fuses (Littelfuse 466/451) are specified to carry 100 % for >= 4 h without opening, but the 75 % rule gives a minimum rating of 3 / 0.75 = 4.0 A, and the ~0.9 ambient re-rating at ~50 C inside the body gives 3 / (0.75 x 0.9) = 4.4 A -> next standard value 5 A. A 3 A CC phase on a 12 Ah pack lasts ~3.5-4 h per charge, i.e. the 3 A element runs at 100 % for hours every cycle -> element aging / nuisance open within the pack's life. What F2 actually protects: with the common-port BMS, CHG+ IS pack + (no diode, §14.2 line 371), so the pack's 20 A (and its much higher short-circuit current before the BMS trips) is available at J12 -> F2 -> J11 -> slip ring -> shell port. The charger (1-3 A, current-limited) can never overload anything; only the pack can. F2 therefore exists to protect the slip-ring rings, the 26-28 AWG ring leads, the shell harness and the charge port against a short, and its rating must be (a) >= charger_max / 0.75 and (b) small enough to clear a ring/harness short before the ring cooks. A 5 A fast-acting chip fuse at a 12 V pack short (tens of amps through ~0.1-0.3 ohm of harness) clears in milliseconds, well before a 2 A ring is damaged, so 5 A satisfies both conditions provided the ring side is at least 3 A-class (see slip-ring finding).
REC: Netlist line 162: change value "3A fuse" -> "5A fuse" for F2 only (keep F4 at 3 A). BOM row F2: MPN 0466005.NR (Littelfuse 466, 1206, 5 A, fast-acting) if the 1206 footprint is kept, or 0451005.MRL if the footprint is changed to NANO2 (see next finding). Fix the doc so §4.1, §13.3 and §14.2 all say "5 A". If the charger is firmly fixed at 1 A forever, 3 A is acceptable (33 %) — but then write that ceiling into §14.
CORRECTED: 1) hardware/netlist/mainboard.py line 162: change "3A fuse" -> "5A fuse" for F2 only; leave line 100 (F4) at "3A fuse". 2) hardware/bom/v10_mainboard_bom.csv line 19: if the netlist's Fuse:Fuse_1206_3216Metric footprint is kept, the row must become F2,1,Fuse 5A (charge path),0466005.NR,1206,Digi-Key,... (both MPN AND Package column change - the current Nano2/0451003.MRL entry does not fit a 1206 land pattern at all); only if the netlist footprint is changed to the KiCad NANO2 pattern (Fuse:Fuse_Littelfuse_NANO2_451_453) should the row be 0451005.MRL / Nano2. Apply the same footprint/package reconciliation to F4 (line 21, stays 3 A: 0466003.NR if 1206). 3) docs/PCB_v10_Design.md line 371 (s14.2): change "3 A fuse" -> "5 A fuse" so s4.1 (line 99), s13.3 (line 337) and s14.2 agree; add to s14 (line 349 row) that the charge path is rated for a <= 3 A charger (5 A fuse at 75 % derating). 4) Note 4 A (0466004.NR / 0451004.MRL) is also a standard value and acceptable at 25 C ambient; 5 A is preferred for the in-body ambient margin. Alternative if the charger is fixed at 1 A forever: keep 3 A but write that 1 A ceiling into s14. The slip-ring's own current rating is not documented anywhere in the repo; verify the ring is >= 3 A-class before relying on the s14.2 "1-3 A ... is fine" statement.
VERIFIER: Evidence verified against the files: hardware/netlist/mainboard.py line 162 is exactly A(two("F2", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", (-40, 18), "CHG+", "CHG_F")), wired J11 pin3 CHG+ -> F2 -> CHG_F -> J12 pin1 (lines 160-164); line 100 is F4 "3A fuse" on the same 1206 footprint. BOM line 19: F2,1,Fuse 3A (charge path),0451003.MRL,Nano2. Doc PCB_v10_Design.md line 99 (s4.1) says "5 A fuse + reverse diode", line 337 (s13.3) says "5 A fuse + SS54", line 371 (s14.2) says "CHG+ -> 3 A fuse -> directly to pack +", line 349 says "12.6 V 1 A (9 h) ... a 12.6 V 2-3 A charger halves the time", and line 371 also says "charge current 1-3 A -> a single slip-ring circuit is fine". So the 3 A charger is inside the stated design envelope and the doc really is inconsistent (5 A twice, 3 A once). Arithmetic checks: 3/0.75 = 4.0 A; 3/(0.75*0.9) = 4.44 A; 3 A on a 3 A fuse = 100 %, and a 3 A CC phase on a 12 Ah pack is ~3-4 h, right at the Littelfuse "100 % for 4 h minimum" spec every cycle. Minor quibble: 4 A is a standard value in both the 466 and 451 series, so "next standard value 5 A" only strictly follows with the temperature rerating; 5 A is still the sensible pick and matches the doc's two 5 A mentions. The slip-ring figures the auditor quotes ("2 A ring", "26-28 AWG leads") do NOT appear in any file (the doc has no slip-ring current rating), but the auditor frames 5 A as conditional on a 3 A-class ring and the doc asserts that at line 371, so this does not change the verdict. Safety of the change: a 5 A fuse does not raise any continuous current (the charger is the limiter), only the trip threshold; downstream hardware is fine (JST-VH 10 A contacts, XT30 30 A, CHG* nets in the 1.2 mm "Power" class at netlist line 228); interrupting rating (50 A) is the same as the existing 3 A part so nothing new is introduced; Littelfuse 466 5 A is 32 VDC rated, fine at 12.6 V. Keeping F4 at 3 A is correct. The one gap in the recommendation: the BOM Package column currently says Nano2 while the netlist footprint is 1206 (the KiCad NANO2 land pattern is ~6.1 x 2.7 mm, a different part) - the recommendation offers the right MPN per footprint but does not say to fix the Package column, so as written the BOM row would remain self-inconsistent and the ordered part would not fit the pads.

## [SHOULD-FIX] Slip ring per-ring rating vs 3 A on the single CHG+ ring: parallel two rings unless the ring is >= 5 A class; doc contradicts itself (§10.1 vs §14.2)
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Netlist line 160-161: J11 4 pins, one ring each for +5V_LED, GND, CHG+, CHG-. Doc §10.1 line 269: "Still to confirm: slip-ring current rating vs charger current (a 2 A charger needs >= 2 A rings; paralleling two rings for CHG+ is the usual fix)" vs §14.2 line 371: "charge current 1-3 A -> a single slip-ring circuit is fine". The actual slip ring is not identified anywhere (PCB_v9_Analysis.md line 486: "Slip ring | 2 | $17.71 | amazon"); at that price it is almost certainly a 12.5 mm or 22 mm capsule (Senring SNM/SNH, Moflon MT, generic) whose common ratings are 2 A per ring with 26-28 AWG leads; 5 A and 10 A/ring variants exist only in the 6-wire 22 mm+ bodies. Arithmetic: 1 A charger on a 2 A ring = 50 % (fine); 3 A on a 2 A ring = 150 % continuous for hours (overheats the brush/ring contact, raises contact resistance, and the 28 AWG lead at 3 A drops ~0.2 V per 0.3 m and dissipates ~0.6 W). Return-path detail: CHG- and GND both end at pack P- (common port), so charge return splits between the CHG- ring and the GND ring, and the GND ring ALSO carries the +5V_LED return (up to the 3 A polyfuse hold). Worst case LEDs-on-while-charging: GND ring ~ (LED 2 A) + (~half of 3 A) ~ 3.5 A on a 2 A ring.
REC: No netlist change is forced, but: (1) identify the slip ring and write its per-ring rating into doc §14.2; (2) if it is a 2 A/ring capsule, wire CHG+ through TWO rings in parallel and CHG-/GND through two rings each (needs a 6- or 8-wire ring; J11 stays 4-pin — parallel the rings in the harness at the board end), OR cap the charger at 1.5 A; (3) have firmware turn the shell LEDs off/dim while CHG detect is true so the GND ring never carries LED + charge return together; (4) delete the "single slip-ring circuit is fine" sentence in §14.2 line 371 and keep §10.1's paralleling statement.
CORRECTED: No board/netlist change. (1) Identify the actual slip ring (Amazon order, PCB_v9_Analysis.md line 486) and write its per-ring rating and wire gauge into PCB_v10_Design.md §14.2 and add it to the v10 BOM as an off-board item. (2) If it is a 2 A/ring capsule: either keep the charger at the listed 1 A (doc line 349 — 50 % of ring rating, no change needed), or for a 2–3 A charger use a 6-wire ring with CHG+ and CHG- on two rings each, paralleled in the harness at the J11 end (J11 stays 4-pin). Derate paralleled rings to ~1.5x (not 2x) because brush contact resistances do not match, so 2 paralleled 2 A rings ≈ 3 A — matching F2. (3) Make F2 match the ring: F2 is Nano2 0451003.MRL 3 A (BOM line 19); if the ring is 2 A/ring and not paralleled, fit 0451002.MRL (2 A, same 1206 footprint, netlist line 162 unchanged) so the fuse, not the brush, is the weak link. (4) Return path: keep the shell charge-port (-) ISOLATED from the shell LED GND (do not bond them in the shell harness); then 100 % of charge return is on the CHG- ring(s) and the GND ring carries only LED return (≤ 3 A, F3 polyfuse). The "GND ring 3.5 A" case only arises if they are bonded; LEDs-off-while-charging in firmware is then optional, not required. (5) Doc: replace §14.2 line 371 "charge current 1–3 A → a single slip-ring circuit is fine" with "≤ 1 A: single ring; 2–3 A: confirm ≥ 3 A/ring or parallel two rings (see §10.1)". While there, fix the adjacent inconsistency at §13.3 line 337 ("5 A fuse + SS54") to match the netlist (F2 3 A, no series diode, per line 371).
VERIFIER: EVIDENCE CHECK (all confirmed from files):
- Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py lines 160-161: J11 "Slip ring" JST_VH_B4P 1x04, pins (1 +5V_LED)(2 GND)(3 CHG+)(4 CHG-). One circuit per function, exactly as claimed. Line 162: F2 "3A fuse" CHG+ -> CHG_F; line 164: J12 XT30 (CHG_F, CHG-). CHG- appears only on J11.4 and J12.2 — not tied to GND on the board. Line 92: F3 "3A polyfuse" +5V_LOGIC -> +5V_LED (BOM line 20: MF-MSMF300, 3 A hold / 6 A trip).
- Doc C:\Users\james\BB8\docs\PCB_v10_Design.md line 269 (§10 item 1) verbatim: "Still to confirm: slip-ring current rating vs charger current (a 2 A charger needs ≥ 2 A rings; paralleling two rings for CHG+ is the usual fix)". Line 371 (§14.2) verbatim: "charge current 1–3 A → a single slip-ring circuit is fine". Line 349 (§14 table): "Slip-ring charge path ≤ 1 A is trivial; a 12.6 V 2–3 A charger halves the time". The contradiction is real: §10.1 says the rating is unconfirmed and paralleling is expected; §14.2 asserts a single circuit is fine up to 3 A with no rating cited.
- Slip-ring identity: nowhere in the repo (grep of *.md/*.py/*.csv/kicad). Only hit is PCB_v9_Analysis.md line 486 "| Slip ring | 2 | 17.71 | amazon |". The v10 BOM lists only the J11 connector (line 52), not the ring. So the per-ring rating is genuinely unknown; the auditor's "almost certainly 2 A/ring" is plausible for that price class but is speculation, not a file fact.

ARITHMETIC: 3 A / 2 A = 150 % — correct. 28 AWG ≈ 0.213 Ω/m → 0.3 m = 0.064 Ω; at 3 A: 0.19 V drop, 0.58 W — auditor's "~0.2 V, ~0.6 W" is right. 1 A charger on 2 A ring = 50 % — correct.

PART OF THE CLAIM THAT IS NOT SUPPORTED: the "return-path split" (GND ring ≈ LED 2 A + ~half of 3 A ≈ 3.5 A). On the board CHG- and GND are separate nets; they meet only at pack P- via J12 -> pack charge lead and J1 XT60 -> F1 -> ... -> board GND. For charge return to flow through the GND ring, the SHELL side must bond the charge-port (-) to the shell LED GND. The netlist/doc define no shell wiring; the whole point of a dedicated CHG- ring (doc line 177) is that they are NOT bonded. If they are kept separate, the CHG- ring carries 100 % of the return and the GND ring carries only the LED return (≤ 3 A polyfuse hold). The "LED 2 A" figure is also unsupported (shell NeoPixel count is not in the files). So the 3.5 A worst case is conditional on an assumption, not established — confidence on that sub-point is low.

SAFETY FOR FAB: the finding forces no netlist/footprint change; J11 stays 4-pin, paralleling happens in the harness. No new board problem is introduced. Note that with the actually listed charger (12.6 V 1 A, doc line 349) the ring is at ≤ 50 % even if it is a 2 A capsule, so this is a harness/doc item for the 2–3 A charger option, not a board blocker — severity "should-fix" is defensible only for the documentation fix and for the moment a >1 A charger is bought.

Verdict: core claim stands (doc self-contradiction is real, ring rating unverified, 3 A on a 2 A ring is 150 %), but the recommendation needs qualification on the return-path/LED item and should use the existing F2 footprint to bound ring current.

## [SHOULD-FIX] 1.2 mm CHG traces: 2.7 A at 10 C rise on 1 oz outer copper — adequate for 3 A (~13 C), NOT adequate on inner layers where the committed route put 104 mm of CHG+/CHG_F; netclass widths were computed for 2 oz
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Netlist line 224 comment: "Trace widths for 2 oz outer copper"; line 228: ("Power", clr 0.25, track 1.20, via 1.0/0.5, [... "CHG*" ...]). BOM row PCB: "2-layer 2oz"; doc §4.1 line 108: "2 oz outer layers"; but the board is now 4-layer (kicad_pcb line 4) with no stackup block, and the PCBWay 4-layer default is 1 oz outer (inner 0.5 oz or 1 oz depending on the order form). IPC-2221 external: I = 0.048 x dT^0.44 x A^0.725 (A in mil^2). 1.2 mm x 34.8 um (1 oz) = 0.0417 mm^2 = 64.6 mil^2; 64.6^0.725 = 20.5. dT 10 C: 0.048 x 2.754 x 20.5 = 2.71 A; dT 20 C: 0.048 x 3.737 x 20.5 = 3.68 A; dT 30 C: 4.40 A. Rise at 3 A: (3 / (0.048 x 20.5))^(1/0.44) = 12.6 C. At 2 oz (129 mil^2, ^0.725 = 33.9): 4.48 A at 10 C. Internal layers, IPC-2221 k = 0.024: 1.2 mm 1 oz = 1.36 A at 10 C, 1.84 A at 20 C, 2.20 A at 30 C; rise at 3 A = 61 C (IPC-2152 says inner layers next to planes are less pessimistic than this, but still roughly half the outer-layer capacity at 0.5 oz). Committed route (git 850cb81, hardware/kicad/mainboard.kicad_pcb): CHG+ 22.8 mm on In2.Cu, CHG_F 81.2 mm on In2.Cu, CHG- 77.1 mm on In2.Cu, +5V_LED 158 mm on In2.Cu, VIN 129 mm on In2.Cu — all at 1.2 mm. The working-tree PCB is currently unrouted (0 segments), so the re-route must not repeat this.
REC: Either order the board with 2 oz outer copper (PCBWay offers it on 4-layer) — then 1.2 mm outer is 4.5 A at 10 C and truly ample — or keep 1 oz and (a) raise the Power netclass track to 2.0 mm (netlist line 228: 1.20 -> 2.00; 2.0 mm 1 oz outer = 0.0696 mm^2 = 108 mil^2, ^0.725 = 29.7, 3.93 A at 10 C) and (b) route CHG+, CHG_F, CHG-, +5V_LED and VIN on F.Cu/B.Cu only (no In2 runs), or widen any inner-layer run of those nets to >= 2.5 mm. Also make the netlist comment on line 224, the BOM PCB row and doc §4.1 agree with the copper weight actually ordered.
CORRECTED: Pick one and make the three sources agree: (A) order PCBWay 4-layer with 2 oz OUTER copper (1.2 mm outer = 4.5 A at 10 C); then in tools/hw/gen_kicad.py line 551 raise min_clearance 0.15 -> 0.20 mm (PCBWay 2 oz min spacing), add a (stackup ...) block with the ordered copper weights, and fix BOM line 60 ("4-layer, 2 oz outer / 1 oz inner, 1.6 mm ENIG") and doc line 108. OR (B) keep 1 oz outer: in hardware/netlist/mainboard.py line 228 change Power track 1.20 -> 2.00 AND replace the pattern "CHG*" with "CHG+", "CHG-", "CHG_F" (otherwise CHG_SENSE, the 47k/10k sense line R26/R27 -> DevKit VN/GPIO39, lines 165-166, gets a 2.0 mm track); line 224 comment -> "1 oz outer: 2.0 mm ~ 3.9 A, 1.2 mm ~ 2.7 A (10 C)"; and constrain the re-route so VIN, BAT+, BAT_F, CHG+, CHG_F, CHG-, +5V_LED, +7V4_SERVO and RP_5V run on F.Cu/B.Cu only (the committed route had 194 mm of +7V4_SERVO and 78 mm of RP_5V on In2 as well as the CHG/VIN/+5V_LED runs) — In2 is the generated solid +5V_LOGIC plane (gen_kicad.py 534-541), so keeping power tracks off it also preserves the plane; any unavoidable In2 run of those nets >= 2.5 mm. In both cases fix BOM line 60 and doc line 108 to the copper weight actually ordered.
VERIFIER: Evidence verified from the files. hardware/netlist/mainboard.py line 224: "# Trace widths for 2 oz outer copper (IPC-2221, ~10 C rise): 3.0 mm ~ 6 A, 2.0 mm ~ 4.5 A, 1.0 mm ~ 2.6 A"; line 228: ("Power", 0.25, 1.20, 1.0, 0.5, ["VIN","BAT*","+5V_LED","+7V4_SERVO","AMP_12V*","CHG*","RP_5V"]). BOM line 60: "PCB,1,2-layer 2oz 1.6mm ENIG". docs/PCB_v10_Design.md line 108: "Copper: 2 oz outer layers ... 2-layer is fine". hardware/kicad/mainboard.kicad_pcb line 4 declares F.Cu/In1.Cu/In2.Cu/B.Cu (4-layer) and contains no (stackup ...) block; tools/hw/fab_outputs.ps1 line 13 exports 4 copper Gerbers and specifies no copper weight anywhere. Working-tree PCB: 0 (segment) entries. Commit 850cb81: 887 segments; parsed per-net In2.Cu lengths = CHG+ 22.8 mm, CHG_F 81.2 mm, CHG- 77.1 mm (In2 only, no vias, TH-to-TH), +5V_LED 158.2 mm, VIN 128.6 mm, all width 1.2 — exactly the claimed numbers. Also on In2.Cu and NOT mentioned by the claimant: +7V4_SERVO 194.1 mm and RP_5V 78.3 mm. gen_kicad.py lines 534-541 generate In2.Cu as a solid +5V_LOGIC plane, so those runs also slot the +5V plane. Arithmetic reproduced: 1.2 mm x 34.8 um = 0.04176 mm^2 = 64.7 mil^2; 64.7^0.725 = 20.5; outer I(10 C) = 0.048 x 2.754 x 20.5 = 2.71 A, I(20 C) = 3.68 A, I(30 C) = 4.39 A; rise at 3 A = (3/0.984)^2.273 = 12.6 C. Inner k = 0.024: 1.36 / 1.84 / 2.20 A; rise at 3 A = (3/0.492)^2.273 = 61 C. 2 oz 1.2 mm: 129 mil^2, ^0.725 = 33.9, 4.49 A at 10 C. 2.0 mm 1 oz outer: 108 mil^2, ^0.725 = 29.8, 3.94 A at 10 C. All correct. If PCBWay's 4-layer inner copper is 0.5 oz (their usual default), 1.2 mm inner = 32.3 mil^2, ^0.725 = 12.4, 0.82 A at 10 C and ~76 C rise at 2 A, ~190 C at 3 A — worse than the claim states. Currents are realistic for the fused path: F2 = 3 A 1206 fuse (line 162) holds 3 A indefinitely; F3 on +5V_LED is a 3 A POLYFUSE (line 92) whose trip is ~2x hold, so +5V_LED can carry ~5-6 A through 158 mm of 1.2 mm inner copper before F3 opens. At the 1 A nominal charger the issue is moot (~5 C inner 1 oz), so should-fix severity is appropriate. The claim stands. The recommendation needs three corrections: (1) the pattern "CHG*" also matches CHG_SENSE (netlist lines 165-166, R26 47k / R27 10k -> DevKit "VN" = GPIO39 per gen_kicad.py line 13), so raising Power to 2.0 mm would put a 2.0 mm track on a high-impedance sense line into a 0.1" header pin; (2) +7V4_SERVO (D24V25F7 2.5 A rail, 194 mm on In2) and RP_5V (78 mm on In2) must be in the outer-only list too; (3) the 2 oz option needs the project min_clearance 0.15 mm (gen_kicad.py line 551) raised to PCBWay's 2 oz minimum (~0.2 mm) and a (stackup) block / order note so the fab actually builds what the netclass assumes.

## [SHOULD-FIX] Shell charge port is permanently live at pack voltage with 20 A behind it (common-port BMS, no diode); F2 is the only protection — port must be a recessed, keyed female and F2 fast-acting
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Established fact: common-port BMS; doc §14.2 line 371: "CHG+ -> 3 A fuse -> directly to pack + (no series diode...)"; netlist has no D3/U8/diode between J12 (line 163-164, CHG_F/CHG-) and J11 (line 161). Hence J12 -> F2 -> J11 pin 3 -> ring -> shell port is at 9-12.6 V whenever the pack is plugged in, even with the robot switched off, and a short at the port or in the ring sees the pack (20 A BMS limit, short-circuit trip only after tens of amps). F2 is upstream of the ring (correct place) and is the ONLY element limiting that energy. A reversed charger (if the port is not keyed) connects the charger output backwards across the pack through F2 — F2 blows; that is acceptable sacrificial protection only if the port is keyed so it cannot happen by accident.
REC: No netlist change (the topology is right for a common-port BMS). Build/doc requirement: the shell charge port must be a recessed female (e.g. panel XT30 female or a centre-positive 5.5/2.5 mm jack) with no exposed live pin; F2 must be FAST-acting (466 series, not 467 slow-blow) so a ring short clears in ms; keep the 20 A ATO at the pack (§14 line 351) as the backup. If an idiot-proof port is not guaranteed, add U8 LM74610 + Q (ideal diode, ~0 V drop, keeps CV accuracy) in the CHG+ path — NOT the SS54 (D3), which drops 0.4 V and ends CV at ~12.2 V as §14.2 already notes.
CORRECTED: Keep the netlist topology (pack -> J12 -> F2 -> J11, no diode) as-is. Before gerber release do the following: (a) BOM fix, not a board fix: change F2 (bom line 19) and F4 (bom line 21) from 0451003.MRL (Nano2, 6.1 x 2.69 mm - does not fit) to Littelfuse 0466003.NR (466 series, 1206, fast-acting, 3 A, 63 VDC, 50 A interrupting) which matches the Fuse_1206_3216Metric footprint at netlist lines 162 and 100 / kicad_pcb lines 306 and 239. Do NOT use 467 (slow-blow). If the §14 line 349 "2-3 A charger" option is ever used, F2 must be 0466004.NR (4 A) because a 3 A fast chip fuse is only good for ~2.25 A continuous. (b) Build/doc hard requirement: shell charge port is a recessed female with no exposed live pin (panel XT30 female, or a 5.5/2.5 mm centre-positive jack) - it is at 9.0-12.6 V with the full pack behind it whenever the pack is plugged in. (c) The LM74610 + N-FET option cannot be added after fab: decide now. Either commit to the keyed/recessed port as the sole reverse protection and delete U8/D3 from the BOM (lines 9 and 16) so nobody fits an SS54, or add U8 (LM74610QDGKRQ1, VSSOP-8) plus its N-FET to the netlist between CHG_F (after F2) and J12 pin 1 with a 2-pad solder-jumper bypass, which also makes R26/R27 CHG_SENSE a genuine charger-present signal (today CHG+ sits at pack voltage permanently so GPIO39 cannot detect a charger). (d) §14 line 351: specify that the XT30 charge lead is spliced to the pack + lead DOWNSTREAM of the 20 A ATO holder, otherwise the ATO does not back up F2 for a ring/port short. (e) Correct the stale doc text at §4.1 line 99, §4.4 line 178 and §13.3 line 337 (5 A fuse + reverse diode / SS54) to match §14.2 line 371 and the netlist (3 A fast fuse, no series diode).
VERIFIER: Every cited piece of evidence is real and says what the claim says. Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py line 160-161: J11 JST-VH pin 3 = CHG+, pin 4 = CHG-; line 162: F2 "3A fuse" Fuse:Fuse_1206_3216Metric CHG+ -> CHG_F; line 163-164: J12 "Charger XT30" pin 1 = CHG_F, pin 2 = CHG-. Lines 165-166: R26 47k CHG+ -> CHG_SENSE, R27 10k CHG_SENSE -> GND. There is no D3, U8 or any diode/FET anywhere between J12 and J11 (grep of the file: only D1 SMBJ15A line 85 and D2 SS14 line 94 exist). The routed board C:\Users\james\BB8\hardware\kicad\mainboard.kicad_pcb line 306 confirms F2 is placed as Fuse_1206_3216Metric with pad 1 on net CHG+ and pad 2 on net CHG_F, i.e. a direct metallic path pack -> J12 -> F2 -> J11 -> ring -> shell port. Design doc C:\Users\james\BB8\docs\PCB_v10_Design.md line 371 (§14.2) reads exactly "CHG+ -> 3 A fuse -> directly to pack + (common-port BMS - no series diode, or the 12.6 V CV phase ends 0.4 V short); reverse protection by the keyed barrel/XT30 at the shell, or an LM74610 ideal-diode controller". Line 351 (§14) has the 20 A ATO at the pack. With the established common-port BMS the pack terminals are always at pack voltage, so the shell port sits at 9.0-12.6 V whenever the pack is plugged in regardless of any robot switch (there is no switch in the netlist anyway; J1 line 80 -> F1 line 82 -> Q1 line 84 is the only input path and the charge lead bypasses it). Arithmetic checks: 12.6 - 0.4 = 12.2 V for the SS54 case matches line 371; the 12 Ah / 144 Wh pack with a 20 A BMS limit is the established fact. The claim that F2 is the only element limiting port/ring short energy is correct: F2 is the only series element between the pack and J11, and the divider R26/R27 (57k) is irrelevant to fault current. The BMS short-circuit threshold ("tens of amps") cannot be verified from the repo; it is a typical value and the claim is hedged, so I accept it with moderate confidence. The reversed-charger reasoning (charger output reverse-parallel across the pack through F2) is sound for a port that is not keyed. "No netlist change" is safe for fab. So the finding stands. The recommendation however has one concrete defect and several gaps that matter for a board going to fab: (1) The BOM already specifies a very-fast-acting fuse for F2 (C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv line 19: 0451003.MRL, package "Nano2") - but Nano2 (Littelfuse 451/453, 6.1 x 2.69 mm body) cannot be soldered to the 1206 footprint that is on the board (pads 1.25 x 1.75 mm at +/-1.4 mm, kicad_pcb line 306). Same mismatch for F4 (BOM line 21 vs netlist line 100 / kicad_pcb line 239). So "466 series" is the right family for the board as routed, but the BOM must actually be changed to it; the reviewer's wording implies the BOM is currently a slow-blow, which it is not. (2) A 3 A fast-acting chip fuse should not carry more than ~75-80 % of rating continuously (~2.25 A); fine for the 1 A charger, but the doc's "2-3 A charger" option (line 349) would open it - choose 4 A if that charger is ever used. (3) The "if not idiot-proof, add U8 LM74610" fallback is not retrofittable after fab - it must be decided before gerber release. (4) The 20 A ATO at the pack only backs up the charge lead if the charge lead is spliced downstream of the ATO; line 351 does not say where the charge lead splits off. (5) Doc sections §4.1 line 99 ("5 A fuse + reverse diode"), §4.4 line 178 ("fused, reverse-blocked") and §13.3 line 337 ("5 A fuse + SS54") still describe the old diode topology and contradict §14.2 line 371 and the netlist; they should be corrected so the fab/build doc does not tell someone to fit a diode. Side observation (not part of this claim): because CHG+ is at pack voltage whenever the pack is connected, CHG_SENSE (R26 line 165 taps CHG+) cannot function as a "charger present" detector as §4.2 line 134 intends; an ideal diode placed between CHG_F and J12 would incidentally fix that.

## [SHOULD-FIX] No diode in the CHG+ path is correct for the common-port BMS; D3/U8 are absent from the netlist as §14.2 intends, but §4.1/§4.4/§13.3 and the BOM still describe a diode/5 A fuse
LENS: TOPOLOGY / CORRECTNESS — charge path vs. common-port 3S BMS pack (J11 -> F2 -> J12, CHG-, CHG_SENSE, J12 connector)
EVIDENCE: Netlist charge path is J11.3 (CHG+) -> F2 (L162) -> CHG_F -> J12.1 (L164): no series diode, confirmed; grep for 'D3', 'D4', 'U8' in mainboard.py: none. Doc §14.2 L371: 'CHG+ -> 3 A fuse -> directly to pack + (common-port BMS -- no series diode, or the 12.6 V CV phase ends 0.4 V short)'. The physics check: an SS54 at 100-300 mA end-of-CV drops ~0.25-0.35 V, so the pack would terminate at ~12.3 V (~90 % SOC) -- the doc's reasoning is right. Stale text: §4.1 L99 'CHG+ -> 5 A fuse + reverse diode', §4.4 L178 '(fused, reverse-blocked)', §13.3 L337 '5 A fuse + SS54'; BOM L16 D3 SS54 'Charge path reverse block (if no U8)', L9 U8 'Optional; else keyed connector only', L19 F2 = 0451003.MRL which is a Littelfuse NANO2 (6.1 x 2.69 mm body) while the netlist footprint is Fuse_1206_3216Metric (L162) -- an assembler following the BOM gets a part that does not fit the pad.
REC: No netlist change for the diode (the SS54 must stay out). If the blocker finding is adopted, U8 becomes FITTED (not optional) and D3 should be deleted from the BOM. Update doc §4.1 L99, §4.4 L178, §13.3 L337 to '3 A fuse + LM74610 ideal diode (no Schottky)'. Fix the F2/F4 BOM line to a real 1206 chip fuse rated >= 32 V DC, 3 A fast-acting (e.g. Littelfuse 0466003.NR -- verify the series' DC voltage and interrupt rating against the ~20 A BMS short-circuit current before ordering; moderate confidence on the exact MPN) or change the two footprints to the NANO2 land pattern.
CORRECTED: 1. Netlist: keep the SS54 (D3) OUT of the CHG+ path (confirmed correct for the common-port BMS). DECIDE PRE-FAB one of:
   (a) Add to hardware/netlist/mainboard.py, in series between F2 and J12 (split CHG_F into CHG_F -> U8/QFET -> CHG_P): U8 LM74610QDGKRQ1 (VSSOP-8, fp Package_SO:VSSOP-8_3.0x3.0mm_P0.65mm), an N-channel MOSFET Vds >= 30 V, Rds(on) <= 20 mOhm (SO-8 / PowerPAK SO-8; anode = CHG_F on source, cathode = CHG_P on drain, LM74610 Vcap/gate per datasheet), and C 1 uF 0603 for the charge pump. Then BOM L9 U8 becomes "Fitted", add the FET + cap lines, delete D3 (L16). With this, CHG_SENSE (R26/R27 on the anode side) reads 0 V when unplugged and the "drive locked while charging" feature works.
   (b) Ship rev A with no blocking element (current netlist). Then the docs must say so: §4.2 L134 and §13.3 L337 change to "CHG_SENSE = pack voltage on rev A (CHG+ is hard-wired to pack + via F2/J12 on a common-port pack); charger-present detection NOT available -- firmware must not gate the drive on GPIO39", BOM L9 U8 and L16 D3 are deleted (not "optional"), and the shell charge port is documented as live at pack voltage whenever the pack is connected (keyed XT30/barrel is the only reverse-polarity protection; F2 is the short protection).
2. Doc text: §4.1 L99, §4.4 L178, §10 L269, §13.3 L337 -> "3 A fuse + LM74610 ideal diode (no Schottky)" ONLY if option (a) is taken; otherwise "3 A fuse, no series element (common-port BMS), keyed connector only". Do not write "LM74610" into the doc while the board has no footprint for it.
3. BOM F2 (L19) and F4 (L21): replace 0451003.MRL (NANO2, 6.1 x 2.7 mm, does not fit the 1206 pads at +/-1.4 mm, 1.25 x 1.75 mm) with Littelfuse 0466003.NR (1206, 3 A, very fast-acting, 32 VAC/VDC, 50 A IR -- verified) and set Package = 1206. If a charger above ~1.5 A will be used (§14 L349 allows 2-3 A), fit F2 as 0466004.NR (4 A) or 0466005.NR (5 A) instead, since a 3 A chip fuse at 2-3 A is at 67-100% of rating and will nuisance-trip; confirm the slip-ring ring current rating at the same time. Alternatively keep the NANO2 MPN and change both footprints in mainboard.py L100/L162 to the Littelfuse NANO2 land pattern (Fuse:Fuse_Littelfuse_NANO2_Horizontal in the KiCad library), but that is a copper change pre-fab, so the BOM fix is the safer path.
VERIFIER: EVIDENCE VERIFIED (all citations real and accurate):
- Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py L161: J11 pin 3 "CHG+" -> net CHG+, pin 4 -> CHG-. L162: F2 "3A fuse", fp "Fuse:Fuse_1206_3216Metric", CHG+ -> CHG_F. L164: J12 pin 1 = CHG_F, pin 2 = CHG-. No diode/ideal-diode element between J11.3 and J12.1. Grep for D3/D4/U8 in mainboard.py: zero hits (D1 L85, D2 L94 are the only diodes; U5/U6/U7 only ICs). Confirmed.
- Doc C:\Users\james\BB8\docs\PCB_v10_Design.md: L99 "CHG+ → 5 A fuse + reverse diode → charger module"; L178 "CHG+ / CHG− pass-through (fused, reverse-blocked)"; L337 "`SLIPRING` CHG± → 5 A fuse + SS54 → charger module"; L371 "CHG+ → 3 A fuse → directly to pack + (common-port BMS — no series diode, or the 12.6 V CV phase ends 0.4 V short) ... or an LM74610 ideal-diode controller". Also L269 (§10) still says "fused + reverse-blocked". All as claimed; §14.2 is the newest decision and contradicts §4.1/§4.4/§10/§13.3.
- BOM C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv: L9 U8 LM74610QDGKRQ1 "Optional; else keyed connector only"; L16 D3 SS54 SMC "Charge path reverse block (if no U8)"; L17 D4 BAT54S; L19 F2 = 0451003.MRL "Nano2"; L21 F4 = 0451003.MRL "Nano2". Confirmed.
- Footprint mismatch is real: KiCad PCB (hardware/kicad/mainboard.kicad_pcb L306, ref F2) has pads 1.25 x 1.75 mm at x = +/-1.4 mm (pad copper spans 0.775..2.025 mm from centre). Littelfuse 451-series NANO2 body is 6.1 x 2.7 mm with ~1.5 mm end caps (caps span ~1.55..3.05 mm). Overlap is ~0.5 mm on each end and the caps overhang the pads by ~1 mm: not solderable on this land pattern. Same for F4 (netlist L100, same footprint).
- Physics: Vishay SS54 Vf is ~0.28-0.35 V at 0.1-0.3 A (25 C). 12.6 - 0.3 = 12.3 V = 4.10 V/cell, roughly 85-90% SOC, so a series Schottky does truncate the CV phase. Auditor's arithmetic is right; the doc's 0.4 V figure is a slightly pessimistic version of the same thing.
- Replacement MPN check (web): Littelfuse 0466003.NR = 466 series 1206 thin-film, 3 A, VERY FAST-ACTING, 32 VAC/32 VDC, 50 A interrupt. It satisfies the stated criteria (>= 32 V DC, fast-acting, 1206, IR 50 A >= ~20 A BMS limit), so the auditor's "moderate confidence" can be raised to high.

WHERE THE RECOMMENDATION IS SUBTLY WRONG / UNSAFE AS WRITTEN:
1. Internal contradiction: "No netlist change" + "U8 becomes FITTED" + doc text "3 A fuse + LM74610 ideal diode". U8 has no footprint in the netlist or the PCB. An LM74610 is a controller that also needs an external N-channel MOSFET and a 1 uF charge-pump cap; none of the three exist on the board. If the doc is rewritten to say "LM74610 ideal diode" while the fabbed rev A has no such part, the doc is stale in the opposite direction. Either add U8 + N-FET + cap to mainboard.py BEFORE fab, or the doc must say "no blocking element on rev A".
2. Missed consequence of "no diode" on a common-port pack: J12 (CHG_F/CHG-) goes to the pack lead, which on a common-port BMS is the same node as the J1 BAT+ lead. Therefore CHG+ (= CHG_F through F2) sits at pack voltage PERMANENTLY, and CHG_SENSE = 12.6 V x 10k/57k = 2.21 V whether or not a charger is plugged in. The doc's §4.2 L134 "CHG_SENSE (charger present) ... charging -> drive force-disabled" and §13.3 L337 "drive locked while charging" cannot work on rev A: CHG_SENSE and VBAT_SENSE (R24/R25 from VIN, L87-88) differ only by the Q1 Rds and F2 drops (tens of mV) plus +/-2% divider tolerance (~+/-250 mV at 12.6 V), so no reliable differential detection either. With the SS54 OR an LM74610 fitted, the CHG+ node is pulled to 0 V by R26/R27 when unplugged and detection works. So the ideal diode is not merely "reverse protection"; it is what makes CHG_SENSE meaningful. The recommended doc text must state this, and the team must decide pre-fab whether to add the U8 footprints or accept no charger detection in rev A (ESP32 GPIO39 is safe either way: 2.21 V < 3.3 V; even the 15 V TVS clamp level gives 2.63 V).
3. Fuse rating margin (adjacent, not a refutation): §14 L349 allows a 2-3 A charger. A 3 A chip fuse at 2-3 A runs at 67-100% of rating; Littelfuse recommends ~25% derating for chip fuses, so a 2 A charger is marginal and a 3 A charger will nuisance-trip F2. If anything above ~1.5 A is planned, use the 4 A or 5 A member of the same series (0466004.NR / 0466005.NR; 466 series covers up to 5 A, moderate confidence on the 5 A part) and confirm the slip-ring ring rating (§10 L269 already flags this).

Net: claim stands (severity should-fix is reasonable for the doc/BOM part; the CHG_SENSE consequence may escalate to a pre-fab copper decision), recommendation needs the corrections below.

## [SHOULD-FIX] Reversed charger at the shell with the as-drawn netlist: pack discharges backwards through the charger until F2 opens; BMS may also trip and drop the whole droid; acceptable only with a polarity-keyed XT30 at the shell, not a barrel jack
LENS: TOPOLOGY / CORRECTNESS — charge path vs. common-port 3S BMS pack (J11 -> F2 -> J12, CHG-, CHG_SENSE, J12 connector)
EVIDENCE: With no blocking element (L160-164), a reversed charger puts charger(-) on CHG+ = pack + and charger(+) on CHG- = pack -. Any CC/CV charger with an output rectifier/blocking diode then forward-conducts: pack + -> F2 -> ring -> charger output diode -> ring -> CHG- -> pack -. Loop resistance ~0.2-0.5 Ohm (two ring circuits + leads) -> 25-60 A from a 12 V pack; F2 (3 A 1206 fast) clears in milliseconds. The common-port BMS sees this as a discharge through its only port: 20 A discharge OCP / short-circuit protection (§14 L348) may latch first or simultaneously -> VIN collapses and the droid reboots (it is charging, so this is tolerable). The board itself is not in the loop (CHG+/CHG- touch only J11, F2, J12, R26 -- L160-166), but after F2 opens CHG+ = charger(-) = -12.6 V w.r.t. GND: CHG_SENSE tries to reach -12.6 x 0.1754 = -2.2 V and GPIO39's ESD structure clamps it at ~-0.5 V, sinking (12.6-0.5)/47k - 0.5/10k = 257 - 50 = ~207 uA -- outside the ESP32 -0.3 V absolute-maximum (see D4 finding). Doc §14.2 L371 says 'reverse protection by the keyed barrel/XT30 at the shell': an XT30 is polarity-keyed; a DC barrel jack is NOT (centre-positive and centre-negative plugs are mechanically identical), so a barrel port offers no protection at all.
REC: Preferred: the ideal diode from the blocker finding (Q2 body diode reverse-biased, U8 holds the FET off when ANODE < CATHODE) makes a reversed charger a no-op -- nothing conducts, F2 survives, the BMS never sees it. If the ideal diode is NOT adopted: (a) the shell port MUST be an XT30 (or another polarity-keyed connector), never a barrel jack; (b) F2 must be a fast-acting chip fuse with interrupt rating >= the BMS short-circuit current at 32 V (not a polyfuse); (c) silkscreen 'F2 3A -- charge path' next to it and note in the runbook that a reversed charger blows F2. No other netlist change.
CORRECTED: Keep the finding (should-fix). Corrected recommendation: PREFERRED - add the LM74610 ideal diode (U8 + N-FET, ANODE = CHG_F charger side, CATHODE = pack side, FET Vds >= 40 V since a reversed charger puts 25.2 V across it) AND, mandatorily, add D4 BAT54S on CHG_SENSE (pin 1 anode -> GND, pin 3 common -> CHG_SENSE, pin 2 cathode -> +3V3): the ideal diode stops the pack discharging but R26 (netlist L165) is upstream of it, so a reversed charger still drives CHG_SENSE toward -2.2 V (207 uA into GPIO39) for as long as it stays plugged in; with the BAT54S the pin sits at ~-0.2 V, within the ESP32 -0.3 V abs max. IF THE IDEAL DIODE IS NOT ADOPTED: (a) the shell port must be an XT30 or other polarity-keyed connector, never a DC barrel jack (doc §14.2 L371 and §4.4 L177-178 should be edited to drop 'barrel'); (b) still add D4 BAT54S as above, because after F2 opens CHG+ sits at -12.6 V until the charger is unplugged; (c) F2 must be a fast-acting chip fuse whose interrupt rating covers the prospective fault current, which is set by loop resistance (~24-60 A from a 12 V pack), e.g. a 50 A @ 32/63 VDC class 1206 fast-acting part - NOT a polyfuse. FIX THE BOM/FOOTPRINT CONFLICT BEFORE FAB EITHER WAY: BOM L19 (F2) and L21 (F4) specify 0451003.MRL, a Littelfuse Nano2 (~6.1 x 2.7 mm, ~4 mm pad pitch) that cannot be soldered onto the 1206 pad pattern gen_kicad.py L269-272 emits for Fuse:Fuse_1206_3216Metric (netlist L162, L100). Either change both BOM lines to a 1206 fast-acting chip fuse (e.g. Littelfuse 466 series 0466003.ER, 3 A, 63 V, ~50 A interrupt - confirm on the current datasheet; or Eaton 3216FF3-R, 3 A, 32 V, 50 A) or change both footprints to the Nano2 pattern and regenerate/re-route. (d) Silkscreen 'F2 3A CHG' beside F2 and a runbook note that a reversed charger blows F2. No other netlist change.
VERIFIER: Claim verified against the files. (1) Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py L160-166: J11 pin3=CHG+, pin4=CHG-; F2 "3A fuse" Fuse:Fuse_1206_3216Metric CHG+->CHG_F; J12 XT30PW pin1=CHG_F, pin2=CHG-; R26 47k CHG+->CHG_SENSE; R27 10k CHG_SENSE->GND. CHG+/CHG-/CHG_F touch nothing else; D3/D4/U8 do not exist in the netlist (grep of the whole file). So there is no blocking element, as claimed. (2) DEVKIT_NETS L13 maps CHG_SENSE to "VN"; doc §4.2 L117/L134 confirms VN = GPIO39 input-only. (3) Doc §14.2 L371 literally says "reverse protection by the keyed barrel/XT30 at the shell, or an LM74610" - the observation that a DC barrel jack is not polarity-keyed is correct. §14 L348 = 20 A BMS. (4) Physics: with charger(-) on CHG+ (=pack+ via F2/J12, common-port) and charger(+) on CHG- (=pack- via the pack harness), any flyback (GND->secondary winding->rectifier->out+) or buck (catch diode->inductor->out+) charger forward-conducts; an output series diode does not block this direction. (5) Arithmetic: 10/(47+10)=0.1754; -12.6*0.1754=-2.21 V; (12.6-0.5)/47k=257 uA; 0.5/10k=50 uA; net 207 uA into the GPIO39 ESD clamp, below the -0.3 V abs-max. 12 V/0.5 Ohm=24 A, 12 V/0.2 Ohm=60 A (loop R is an estimate). A 3 A fast fuse at 8-20x rating clears in ms, before a typical BMS OCP delay; the "BMS may trip" part is a hedge, not wrong. Minor imprecision: the board's CHG_F/CHG+/CHG- traces ARE in the fault loop (1.2 mm Power class, L228) - only GND/logic are not; I^2t over a few ms is negligible, so not a refutation. The finding stands at should-fix. Three problems with the recommendation as written: (a) the ideal diode does not make the fault a no-op for CHG_SENSE - R26 is upstream of the diode so CHG_SENSE still goes to -2.2 V, and because nothing blows the 207 uA negative injection into GPIO39 now persists indefinitely, so the D4 BAT54S clamp must be a mandatory companion; (b) BOM C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv L19 specifies F2 = 0451003.MRL (Littelfuse Nano2, ~6.1x2.7 mm body) while the netlist footprint is 1206 and tools/hw/gen_kicad.py L269-272 emits 1206 pads (1.2x1.8 mm at +/-1.4 mm) - that part cannot be fitted; F4 (BOM L21 / netlist L100) has the same mismatch; (c) "interrupt rating >= the BMS short-circuit current" is unmeetable with a 1206 chip fuse (BMS SC thresholds are typically 60-150 A; 1206 chip fuses interrupt ~50 A) - the prospective fault current here is bounded by loop resistance (~24-60 A), so spec against that.

## [SHOULD-FIX] Add the D4 BAT54S clamp on CHG_SENSE (and optionally VBAT_SENSE) -- the BOM lists it, the netlist omits it, and CHG_SENSE is wired to an exposed external port on an input-only GPIO with no internal protection
LENS: TOPOLOGY / CORRECTNESS — charge path vs. common-port 3S BMS pack (J11 -> F2 -> J12, CHG-, CHG_SENSE, J12 connector)
EVIDENCE: BOM L17: D4 BAT54S 'ADC clamps (battery sense / CHG_SENSE)'; doc §4.1 L95 and §4.2 L132 '47k/10k + 3.3 V clamp'. Netlist L165-166 and L87-88 have bare dividers only, no clamp, no filter cap. GPIO39/GPIO35 are input-only (doc §4.2 L119) with ESP32 absolute-maximum -0.3 V..3.6 V. Fault arithmetic: reversed charger (previous finding) drives CHG+ to -12.6 V -> ~207 uA into the pin's substrate diode; a 24 V supply mis-plugged at the shell port -> CHG_SENSE = 24 x 0.1754 = 4.2 V, (4.2-3.6)/8.2k equivalent ~ 60-100 uA into the 3V3 rail clamp. Neither is board-lethal but both violate the datasheet limits on a pin that also has 12.6 V behind 47k continuously. One BAT54S clamps one node; the BOM quantity (1) only covers one of the two dividers.
REC: Add (SOT-23 BAT54S: pin1 = anode of D1, pin3 = common, pin2 = cathode of D2):
  A(dict(ref="D4", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(-33, 21), rot=0, side="F", pins=[(1,"A","GND"),(2,"K","+3V3"),(3,"COM","CHG_SENSE")]))
  A(two("C30", "100n", "cap", "Capacitor_SMD:C_0603_1608Metric", (-33, 24), "CHG_SENSE", "GND"))   # source impedance 47k||10k = 8.2k -> RC = 0.8 ms, also ESD/ring noise
Optional second clamp D5 BAT54S + C31 100n on VBAT_SENSE (same pattern, at (44, 38)); update BOM D4 qty to 2.
CORRECTED: Add BOTH clamps (the design doc §4.2 L132 / §13.3 L336 assigns the BAT54 clamp to GPIO35 battery sense, and the BOM note covers both dividers), not just CHG_SENSE. Keep the auditor's pinout (BAT54S: pin 1 = anode of D1 -> GND, pin 2 = cathode of D2 -> +3V3, pin 3 = common -> signal). Insert in C:\Users\james\BB8\hardware\netlist\mainboard.py:

After L88 (R25), in the "protection / input" block:
A(dict(ref="D4", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(44, 38), rot=0, side="F", pins=[(1,"A","GND"),(2,"K","+3V3"),(3,"COM","VBAT_SENSE")]))
A(two("C29", "100n", "cap", "Capacitor_SMD:C_0603_1608Metric", (47, 41), "VBAT_SENSE", "GND"))

After L166 (R27), in the slip-ring / charger block:
A(dict(ref="D5", value="BAT54S", kind="ic3", fp="Package_TO_SOT_SMD:SOT-23", at=(-33, 21), rot=0, side="F", pins=[(1,"A","GND"),(2,"K","+3V3"),(3,"COM","CHG_SENSE")]))
A(two("C30", "100n", "cap", "Capacitor_SMD:C_0603_1608Metric", (-33, 24), "CHG_SENSE", "GND"))

(Coordinates are preferences only; gen_kicad.py place_all() spirals parts to a free spot, so exact values are not load-bearing. C29/C30 are unused refs; the BOM's C10-C25 100nF line already has spare quantity.)

BOM C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv L17: change to "D4-D5,2,BAT54S,BAT54S,SOT-23,Digi-Key,ADC clamps: D4 VBAT_SENSE (GPIO35), D5 CHG_SENSE (GPIO39)". Then regenerate the KiCad project from the netlist and re-route the two small areas (net CHG_SENSE near R26/R27, net VBAT_SENSE near R24/R25, plus a short +3V3 stub to each clamp). Note for the record: the reversed-charger -12.6 V excursion only occurs with the pack disconnected / BMS open (common-port BMS ties the charge lead to BAT+); the clamp is still warranted because the doc specifies it and CHG_SENSE leaves the board via the slip ring and shell port.
VERIFIER: Evidence verified against the files. Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py L87-88 (R24 47k VIN->VBAT_SENSE, R25 10k VBAT_SENSE->GND) and L165-166 (R26 47k CHG+->CHG_SENSE, R27 10k CHG_SENSE->GND) are bare dividers; no D3/D4/U8, no BAT54, no filter cap anywhere in the netlist. L13 maps DevKit "VN"->CHG_SENSE (GPIO39) and "D35"->VBAT_SENSE (GPIO35). BOM C:\Users\james\BB8\hardware\bom\v10_mainboard_bom.csv L17 is exactly "D4,1,BAT54S,BAT54S,SOT-23,Digi-Key,ADC clamps (battery sense / CHG_SENSE)". Design doc C:\Users\james\BB8\docs\PCB_v10_Design.md calls for the clamp three times: §4.1 L95 "Battery sense 47k/10k + 3.3 V clamp -> GPIO35", §4.2 L132 "47k/10k divider (12.6 V -> 2.21 V), BAT54 clamp to 3V3", §13.3 L336 "47k / 10k divider + 3.3 V clamp -> GPIO35"; §4.2 L119 confirms "34/35/36/39 are input-only". So the doc/BOM vs netlist discrepancy is real.

Arithmetic checks: 10k/(47k+10k) = 0.1754; 12.6 V x 0.1754 = 2.21 V (matches doc); 24 V x 0.1754 = 4.21 V; 47k||10k = 8.25k; (4.21-3.6)/8.25k = 74 uA (inside the claimed 60-100 uA); -12.6 V x 0.1754 = -2.21 V Thevenin, (2.21-0.5)/8.25k = 207 uA (matches, assuming a ~0.5 V substrate-diode drop). RC = 8.25k x 100n = 0.82 ms, correct. One caveat on the reversed-charger scenario: with a common-port BMS the charge lead is the same node as BAT+, so while the pack is connected a reversed charger fights the pack and CHG+ is held near +pack voltage (charger current-limits / F2 opens); CHG+ only reaches -12.6 V if the pack is absent or the BMS has opened. The negative-excursion case is therefore conditional, but the claim does not depend on it: the doc-specified clamp is simply missing, the node leaves the board through the slip ring and shell port, and the pin is input-only with no pull structure.

Recommendation mechanics verified: BAT54S (series) pinout is pin 1 = anode of D1, pin 2 = cathode of D2, pin 3 = common (K1/A2), so pin1->GND, pin2->+3V3, pin3->signal is the correct clamp hookup. Package_TO_SOT_SMD:SOT-23 exists in the installed KiCad 10 library (C:\Program Files\KiCad\10.0\share\kicad\footprints\Package_TO_SOT_SMD.pretty\SOT-23.kicad_mod, 3 SMD pads) and gen_kicad.py find_lib_footprint()/embed_lib_footprint() (L148-185) embeds it; kind="ic3" is the same kind used by Q1/U5 and lib_symbol() accepts any pin names. The two() helper signature (ref, value, kind, fp, at, n1, n2) is used correctly. place_all() (gen_kicad.py L367-414) spirals every non-module part to the nearest free spot with a 2.5 mm gap, so the preferred coordinates (-33,21)/(-33,24), which sit 1 mm above PS1's 25.9x28.4 courtyard (y up to 18.2) and 3 mm from R27, cannot produce an overlap. C30 does not collide with any existing ref (netlist uses C1-C12, C26-C28). Adding the clamp introduces no new hazard: the upper diode never conducts in normal operation (2.21 V vs 3.3+0.3 V), BAT54S reverse leakage at 3 V is well under 1 uA at the temperatures inside the shell (<~10 mV error across the 8.25k source), and if +3V3 is down while CHG+ is present the clamp only sources ~230 uA into the unpowered rail, which is benign and actually protects the unpowered ESP32 pin.

Only adjustment: the auditor frames the VBAT_SENSE clamp as optional, but the design doc (L95, L132, L336) designates the clamp for GPIO35/battery sense specifically and the BOM note covers both dividers. Both should be added; the board is going to fab and adding a second SOT-23 costs nothing. Severity "should-fix" is appropriate (not fab-blocking, but it is a documented protection feature silently dropped from the netlist on two externally-exposed, input-only ADC pins). This requires regenerating the KiCad project and re-routing the affected area, which is a workflow cost, not a refutation.

## [NICE-TO-HAVE] CHG_SENSE is routed as a 1.2 mm Power-class track (pattern 'CHG*' also matches the sense net)
LENS: ESP32 ADC INPUTS (CHG_SENSE / GPIO39-VN, VBAT_SENSE / GPIO35-D35)
EVIDENCE: mainboard.py L228 Power netclass patterns include "CHG*"; CHG_SENSE matches. HEAD PCB: all 10 CHG_SENSE segments are width 1.2 (vs 0.3 for VBAT_SENSE and S2S_POS), 74.5 mm of 1.2 mm track with 0.25 mm clearance for a microamp signal on a board that still has routing opens (commit 850cb81). Electrically harmless; it wastes routing channels and adds coupling area to neighbours.
REC: L228: replace "CHG*" with "CHG+", "CHG-", "CHG_F" (and "CHG_PACK" if D3 is added) so CHG_SENSE falls back to Default (0.3 mm).
VERIFIER: Evidence is real and exact. hardware/netlist/mainboard.py L228 defines Power class (clearance 0.25, track 1.20, via 1.0/0.5) with pattern "CHG*"; tools/hw/gen_kicad.py L550-561 copies these patterns verbatim into mainboard.kicad_pro netclass_patterns (confirmed present). KiCad's own DSN export (hardware/kicad/temp-freerouting.dsn L1804-1810) lists "(class Power ... CHG+ "CHG-" CHG_F CHG_SENSE RP_5V VIN ... (width 1200))", proving the glob captures CHG_SENSE. Parsing the HEAD (850cb81) mainboard.kicad_pcb: CHG_SENSE = 10 segments (8 on F.Cu = 56.0 mm, 2 on In2.Cu = 18.6 mm), all width 1.2, total 74.5 mm, plus one 1.0/0.5 via; VBAT_SENSE = 17 segs all 0.3 mm (57.1 mm), S2S_POS = 15 segs all 0.3 mm (94.2 mm), ESTOP_SENSE = 20 segs all 0.3 mm. Auditor's figures match exactly. Net current is ~12.6 V / (47k+10k) = 221 uA, so 0.3 mm Default is electrically trivial; the only effect of the 1.2 mm/0.25 mm class is wasted routing channels on a board that had routing opens. Recommendation is correct and safe: the same NETCLASSES list already relies on wildcard-free exact patterns ("VIN", "+5V_LED", "RP_5V") which demonstrably work (DSN Power class contains exactly those nets and no spurious prefix matches); KiCad's netclass matcher escapes '+' and anchors the pattern, so "CHG+" matches only net CHG+, and "CHG-"/"CHG_F" contain no metacharacters. No remaining pattern re-captures CHG_SENSE, so it falls to Default (0.30/0.20/0.8-0.4), identical to VBAT_SENSE/ESTOP_SENSE/S2S_POS which route without issue. The working copy mainboard.kicad_pcb currently has 0 segments (regenerated/unrouted), so the change applies at the next gen_kicad.py + DSN export + autoroute with no rework; on the HEAD-routed copy the existing 1.2 mm segments keep their per-segment widths, so there is no DRC regression either way. Severity "nice-to-have" is appropriate; this is not a fab blocker.

## [NICE-TO-HAVE] Power-class via (1.0 mm / 0.5 mm drill) carries the full 3 A alone at each layer change in the CHG path
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Netlist line 228: Power via dia 1.0, drill 0.5. Committed route: CHG+ 2 vias, CHG_F 2 vias, +5V_LED 4 vias, each a single via per transition. Barrel area with 25 um plating: pi x 0.5 x 0.025 = 0.0393 mm^2 = 60.9 mil^2; ^0.725 = 19.7. IPC-2221 external-equivalent: 0.048 x 2.754 x 19.7 = 2.6 A at 10 C, 3.5 A at 20 C — about the same as a 1.1 mm 1 oz trace, so ~15 C at 3 A. Acceptable, but it is the narrowest point in the path and PCBWay's minimum plating is 20 um (-> 2.2 A at 10 C).
REC: When re-routing: use two vias per layer transition on CHG+/CHG_F/CHG-/+5V_LED/VIN, or raise the Power netclass via to 1.2 mm / 0.6 mm drill (netlist line 228: via 1.0 -> 1.2, drill 0.5 -> 0.6). Routing CHG nets outer-only (previous finding) removes the vias entirely.
CORRECTED: Keep the finding but fix two points. (a) Do not call the via the narrowest point: in the committed route (HEAD 850cb81) the In2.Cu 1.2 mm segments on CHG+ (6), CHG_F (7), CHG- (8 of 8) and +5V_LED (17) rate ~1.4 A at 10 C by the same IPC-2221 internal formula (1 oz inner), which is the real bottleneck - route CHG+/CHG_F/CHG-/+5V_LED on F.Cu/B.Cu (2 oz) only; that removes both the inner segments and most vias. (b) Where a layer change is unavoidable, use two paralleled Power-class vias per transition (~1.65-2x); the netlist line 228 change 'via 1.0 -> 1.2, drill 0.5 -> 0.6' is safe (0.3 mm annular, min_via 0.6/0.3 in gen_kicad.py line 551) but only buys +14% ((0.6/0.5)^0.725), so treat it as a supplement, not an alternative. Apply the line-228 edit before re-running the autorouter on the currently unrouted working-tree .kicad_pcb, and on the PCBWay order select 25 um hole copper (IPC Class 3) rather than the 20 um default - that alone restores 2.6 A @10 C per via. Also update BOM line 60 ('2-layer 2oz') to the actual 4-layer, 2 oz outer / 1 oz inner spec so the fab order matches the design.
VERIFIER: Evidence is real: C:\Users\james\BB8\hardware\netlist\mainboard.py line 228 is ("Power", 0.25, 1.20, 1.0, 0.5, [... "CHG*" ...]) = via 1.0 mm / drill 0.5 mm, and tools/hw/gen_kicad.py lines 550-561 copy these into the .kicad_pro netclasses (board min_via 0.6/0.3 at line 551, so 1.2/0.6 is legal). Parsing the committed HEAD (850cb81) mainboard.kicad_pcb gives 110 vias: CHG+ 2, CHG_F 2, +5V_LED 4, VIN 7, CHG- 0, all 1.0/0.5 F.Cu-B.Cu through vias at positions 10-90 mm apart, i.e. one via per transition exactly as claimed (the working-tree .kicad_pcb is regenerated/unrouted: 0 vias, 0 segments). Arithmetic verified: pi*0.5*0.025 = 0.0393 mm^2 = 60.9 mil^2; 60.9^0.725 = 19.67; 0.048*2.754*19.67 = 2.60 A @10 C; 3.53 A @20 C; rise at 3 A = (3/0.944)^(1/0.44) = 13.8 C; at 20 um plating 48.7 mil^2 -> 2.21 A; 1.1 mm x 35 um = 0.0385 mm^2 matches. PCBWay hole-copper options are 20/25/30/35/50/55/70 um, 20 um minimum/default (pcbway.com helpcenter) - correct. Design doc line 108 says 2 oz outer, line 349/371 charge current 1 A nominal, 2-3 A max, consistent with F2 3 A. Recommendation is safe for fab: 1.2/0.6 via has 0.3 mm annular ring (> PCBWay 0.15 mm), aspect ratio 2.7:1, no new fit constraints versus the existing 1.0 mm via at any Power-net connector (J11 3.96 mm pitch, J12/J1 XT, fuses). Two corrections needed though: (1) 'narrowest point in the path' is false for the committed route - CHG+ has 6, CHG_F 7, CHG- 8 of 8 and +5V_LED 17 segments on In2.Cu at 1.2 mm; by the same IPC-2221 method (internal k=0.024, 1 oz inner since inner weight is never specified - BOM line 60 still says '2-layer 2oz') that is 0.042 mm^2 = 65.1 mil^2 -> 1.36 A @10 C (~60 C at 3 A), far worse than the via's 2.6 A; the 2 oz outer 1.2 mm trace is 4.5 A. (2) The 1.2/0.6 option only scales ampacity by (0.6/0.5)^0.725 = 1.14 (2.2 -> 2.5 A at 20 um, 2.6 -> 3.0 A at 25 um), so it is not an equivalent alternative to paired vias (~1.65-2x). Severity 'nice-to-have' is right: at the 1 A nominal charger the via rise is ~1 C, at 3 A/20 um ~20 C.

## [NICE-TO-HAVE] +5V_LED via F3 MF-MSMF300 (3.0 A hold / 6.0 A trip, 6 V max) on the other ring: OK on the 5 V rail, but match it to the ring rating and note it is slow and shares the 5 A buck with logic
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Netlist line 92: F3 "3A polyfuse" 1812 between +5V_LOGIC and +5V_LED; BOM row F3: MF-MSMF300 "+5V_LED to slip ring". Bourns MF-MSMF300-2: Ihold 3.0 A, Itrip 6.0 A, Vmax 6 V, ~0.02-0.04 ohm (0.06-0.12 V drop at 3 A). 6 V max vs 5.0-5.25 V rail: fine, but it must never be reused on a 12 V net. Polyfuse trip time at 2 x Ihold is seconds, at 1.5 x Ihold tens of seconds: a shell-LED short pulls 6 A+ from PS1 (D24V50F5, 5 A rated, line 72) which also feeds +5V_LOGIC (DevKit VIN, RP2350 5V, DFPlayer) -> logic rail sags / buck hiccups and both MCUs reset until the polyfuse trips. If the ring is a 2 A type, 3 A hold is above the ring rating. 1.2 mm +5V_LED copper: same 2.7 A at 10 C as above; at a realistic LED load of 1-2 A it is fine.
REC: If the slip ring is 2 A/ring: BOM F3 -> MF-MSMF200-2 (2.0 A hold, 4 A trip, 8 V) and netlist line 92 value "2A polyfuse". If rings are paralleled or >= 5 A rated: keep MF-MSMF300-2. Optionally swap F3 for a fast 1206 chip fuse (0466003.NR, same 1206 family as F2/F4 if option A is chosen) so a shell short does not drag +5V_LOGIC for seconds; or keep the polyfuse and accept the reset. Firmware: LEDs off while charging (see slip-ring finding).
CORRECTED: Not fab-blocking as long as the polyfuse stays. Do this: (1) Before gerbers, DECIDE whether F3 remains a polyfuse (design intent, §10 item 1 / §13.3) or becomes a one-shot fast fuse. If polyfuse: leave netlist line 92 footprint Fuse:Fuse_1812_4532Metric exactly as is; the MF-MSMF300-2 vs MF-MSMF200-2 choice is BOM-only (same 1812 land) and can be made after fab once the slip-ring per-ring rating is confirmed (it is still marked unconfirmed in PCB_v10_Design.md §10 item 1; the v9 BOM row in PCB_v9_Analysis.md line 486 has no rating). Size it against the TOTAL +5V_LED load: J10 body NeoPixels (line 158) + shell NeoPixels through J11, not the shell alone; with Ihold derating ~0.8x at 50 C, only go to MF-MSMF200-2 (2.0 A hold / 4.0 A trip / 8 V) if that combined load is comfortably under ~1.5 A and the ring is 2 A class; otherwise keep MF-MSMF300-2 (Vmax 6 V — never reuse on VIN/12 V nets). (2) If instead a fast fuse is wanted so a shell short cannot drag +5V_LOGIC (DevKit VIN line 15, RP_5V line 94, DFPlayer line 31) for seconds: change line 92 to two("F3", "3A fuse", "fuse", "Fuse:Fuse_1206_3216Metric", (-44, -18), "+5V_LOGIC", "+5V_LED") and BOM F3 -> 0466003.NR (Littelfuse 466, 1206, 3 A fast) BEFORE regenerating gerbers — gen_kicad.py lines 269-272 make 1812 pads (centres +/-2.05 mm) that a 3.2 mm 1206 part cannot bridge, so this cannot be done after fab. Update the BOM note from "+5V_LED to slip ring" to "+5V_LED (J10 body + J11 shell pixels)". (3) Firmware: blank shell/body pixels while CHG_SENSE indicates a charger, as the slip-ring finding already says.
VERIFIER: Evidence verified against the files. Netlist C:\Users\james\BB8\hardware\netlist\mainboard.py line 92 is exactly: two("F3", "3A polyfuse", "fuse", "Fuse:Fuse_1812_4532Metric", ..., "+5V_LOGIC", "+5V_LED") — so F3 hangs off the +5V_LOGIC net, not a separate PS1 output. Line 72-73: PS1 "Pololu D24V50F5 5V 5A", VOUT -> +5V_LOGIC. Line 15: DevKit VIN = +5V_LOGIC; line 94: D2 SS14 +5V_LOGIC -> RP_5V (RP2350-Zero 5V); line 31: DFPlayer VCC = +5V_LOGIC. BOM row 20: "F3,1,Polyfuse 3A,MF-MSMF300,1812,Digi-Key,+5V_LED to slip ring"; BOM row 10: PS1 "+5V_LOGIC and +5V_LED". So "shares the 5 A buck with logic" is literally true from the netlist. Design doc §13.3 row "5 V" and §10 item 1 confirm the polyfuse is the designer's stated intent ("own polyfuse + 1000 uF", "polyfused LED out"). Bourns MF-MSMF300-2 figures (Ihold 3.0 A, Itrip 6.0 A, Vmax 6 V, Rmin ~0.02 ohm / R1max ~0.045 ohm) match the datasheet as I know it; 3 A x 0.02-0.04 ohm = 0.06-0.12 V is correct. MF-MSMF200-2 (2.0 A / 4.0 A / 8 V, same 1812 land) is a correct drop-in. Copper: IPC-2221 external, 1 oz, 10 C rise, 1.2 mm (47 mil x 1.37 mil = 64.7 mil^2): 0.048 x 10^0.44 x 64.7^0.725 = 0.048 x 2.754 x 20.55 = 2.72 A, so "2.7 A" is right for the 1 oz outer copper PCBWay uses by default on 4-layer (note netlist line 224 still assumes 2 oz and BOM row 60 still says "2-layer 2oz" — stale, but not this finding). The slip-ring rating is genuinely unresolved in the repo: §10 item 1 says "Still to confirm: slip-ring current rating vs charger current (a 2 A charger needs >= 2 A rings...)" and the only other reference (docs\PCB_v9_Analysis.md line 486 "Slip ring | 2 | 17.71 | amazon") has no rating, so the auditor's conditional framing is honest, not a fabrication. The shell LED count is not documented anywhere (firmware only defines NUM_PIXELS for the dome), so "1-2 A realistic" is an assumption — uncertainty noted. The failure scenario (shell short -> >6 A demanded from a 5 A buck -> +5V_LOGIC sags -> DevKit/RP2350 brown-out until F3 trips, or never trips if the buck current-limits below Itrip) is physically sound; I cannot confirm the D24V50F5 current-limit value, which determines which of the two bad outcomes occurs. Severity "nice-to-have" is defensible because the polyfuse is a documented design decision. Two things the recommendation gets subtly wrong for a board about to be fabbed: (1) the optional "swap F3 for a fast 1206 chip fuse (0466003.NR)" is NOT a BOM-only change — gen_kicad.py lines 269-272 generate 1812 pads at +/-2.05 mm with 1.2 x 3.4 mm pads, a 3.2 mm-long 1206 part will not bridge them, so that option requires editing line 92's footprint to Fuse:Fuse_1206_3216Metric BEFORE gerbers are regenerated, or it is dead post-fab; (2) the finding omits that J10 "Body NeoPixel" (line 158, pin 1 = +5V_LED) is also on +5V_LED, so F3 carries body + shell pixels, and an MF-MSMF200-2 (Ihold derates ~0.8x at 50 C -> ~1.6 A) risks slow nuisance trips if the combined pixel load is near 2 A; also a polyfuse passes up to Itrip (4 A) for tens of seconds, so it only loosely protects a 2 A ring either way. The MSMF300 vs MSMF200 choice is the same 1812 land, so it is a BOM-only decision and not fab-blocking.

## [NICE-TO-HAVE] Netclass pattern "CHG*" also captures CHG_SENSE, routing a 47k/10k divider node at 1.2 mm
LENS: CURRENT & CONNECTORS (charge path F2/J11/J12, slip ring, +5V_LED/F3, CHG copper)
EVIDENCE: Netlist line 228: Power patterns include "CHG*"; nets CHG_SENSE (line 13, 165-166). Committed route: CHG_SENSE 56 mm F.Cu + 18.6 mm In2.Cu at 1.2 mm width. Harmless electrically but wastes routing room next to J11/J12/PS1 and gives the high-impedance ADC node a larger antenna.
REC: Netlist line 228: replace "CHG*" with "CHG+", "CHG-", "CHG_F" so CHG_SENSE falls back to the Default 0.30 mm class.
VERIFIER: Evidence verified against the files. (1) C:\Users\james\BB8\hardware\netlist\mainboard.py line 228 is exactly: ("Power", 0.25, 1.20, 1.0, 0.5, ["VIN", "BAT*", "+5V_LED", "+7V4_SERVO", "AMP_12V*", "CHG*", "RP_5V"]). Line 13 maps DevKit "VN" -> "CHG_SENSE"; lines 165-166 define R26 47k CHG+ -> CHG_SENSE and R27 10k CHG_SENSE -> GND. The only CHG-prefixed nets in the netlist are CHG+, CHG-, CHG_F (lines 161-164) and CHG_SENSE (165-166). (2) gen_kicad.py lines 550-561 pass these strings verbatim into the .kicad_pro "netclass_patterns"; KiCad evaluates them as anchored wildcards, so "CHG*" does match CHG_SENSE. Confirmed in the HEAD .kicad_pro (pattern "CHG*" -> Power). (3) Arithmetic confirmed by parsing the committed (HEAD 850cb81) mainboard.kicad_pcb: CHG_SENSE has 56.0 mm on F.Cu and 18.6 mm on In2.Cu, all at width 1.2 mm, plus one 1.0/0.5 mm (Power-class) via - exactly the auditor's numbers. The analogous divider node VBAT_SENSE (R24/R25, same 47k/10k topology) is routed at 0.30 mm (19.0 B.Cu + 18.2 F.Cu + 19.9 In2.Cu) with 0.8/0.4 Default vias, confirming the inconsistency is purely an artifact of the wildcard. Side effect the auditor did not mention: the 18.6 mm In2.Cu run is on the +5V plane layer, so the 1.2 mm track + 0.25 mm clearance carves a ~1.7 mm-wide slot out of the +5V plane for no reason. (4) Recommendation safety: literal patterns containing '+' demonstrably work in this flow - "+5V_LED" (line 228) produced 1.2 mm tracks (37.5 F / 102.3 B / 158.2 In2 mm), so "CHG+" will be honored the same way; "CHG-" and "CHG_F" are plain literals. The three explicit names cover every current-carrying charge net; CHG_SENSE then falls to Default (0.30 mm track, 0.20 mm clearance, 0.8/0.4 via), which is well above the project min rules (0.15 mm clearance, 0.2 mm track) and PCBWay 4-layer capability. No electrical change - a 47k/10k divider node (Thevenin ~8.2k) carries microamps. Note: the working-tree mainboard.kicad_pcb is currently regenerated and unrouted (0 segments), so the change takes effect automatically on the next route; if the HEAD-routed board were sent as-is, the netclass edit alone changes nothing (the widths are baked into the existing tracks) - either way it cannot introduce a fab problem. Severity "nice-to-have" is appropriate; the claim stands as stated.

